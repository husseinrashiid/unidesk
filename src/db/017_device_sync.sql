-- Generated by scripts/sync-schema.mjs. Do not edit capture triggers by hand.
CREATE TABLE sync_context (id INTEGER PRIMARY KEY CHECK(id=1), device_id TEXT NOT NULL, clock INTEGER NOT NULL DEFAULT 0, applying INTEGER NOT NULL DEFAULT 0, cursor INTEGER NOT NULL DEFAULT 0);
INSERT INTO sync_context(id,device_id) VALUES(1,lower(hex(randomblob(16))));
CREATE TABLE sync_outbox (table_name TEXT NOT NULL, record_key TEXT NOT NULL, seq INTEGER NOT NULL, payload TEXT, PRIMARY KEY(table_name,record_key));
CREATE TABLE sync_versions (table_name TEXT NOT NULL, record_key TEXT NOT NULL, version INTEGER NOT NULL, PRIMARY KEY(table_name,record_key));
CREATE TABLE sync_conflicts (table_name TEXT NOT NULL, record_key TEXT NOT NULL, local_payload TEXT, remote_payload TEXT, remote_version INTEGER NOT NULL, PRIMARY KEY(table_name,record_key));
CREATE TABLE sync_blobs (file_id TEXT PRIMARY KEY, hash TEXT NOT NULL, local_signature TEXT NOT NULL DEFAULT '');
CREATE TABLE sync_preferences (key TEXT PRIMARY KEY, value TEXT NOT NULL);
CREATE TRIGGER sync_semesters_insert AFTER INSERT ON "semesters" WHEN (SELECT applying FROM sync_context WHERE id=1)=0 BEGIN
UPDATE sync_context SET clock=clock+1 WHERE id=1;
INSERT INTO sync_outbox(table_name,record_key,seq,payload) VALUES('semesters',json_array(NEW."id"),(SELECT clock FROM sync_context WHERE id=1),json_object('id',NEW."id",'name',NEW."name",'start_date',NEW."start_date",'end_date',NEW."end_date",'status',NEW."status",'created_at',NEW."created_at")) ON CONFLICT(table_name,record_key) DO UPDATE SET seq=excluded.seq,payload=excluded.payload;
END;
CREATE TRIGGER sync_semesters_update AFTER UPDATE ON "semesters" WHEN (SELECT applying FROM sync_context WHERE id=1)=0 AND (OLD."id" IS NOT NEW."id" OR OLD."name" IS NOT NEW."name" OR OLD."start_date" IS NOT NEW."start_date" OR OLD."end_date" IS NOT NEW."end_date" OR OLD."status" IS NOT NEW."status" OR OLD."created_at" IS NOT NEW."created_at") BEGIN
UPDATE sync_context SET clock=clock+1 WHERE id=1;
INSERT INTO sync_outbox(table_name,record_key,seq,payload) VALUES('semesters',json_array(NEW."id"),(SELECT clock FROM sync_context WHERE id=1),json_object('id',NEW."id",'name',NEW."name",'start_date',NEW."start_date",'end_date',NEW."end_date",'status',NEW."status",'created_at',NEW."created_at")) ON CONFLICT(table_name,record_key) DO UPDATE SET seq=excluded.seq,payload=excluded.payload;
END;
CREATE TRIGGER sync_semesters_delete AFTER DELETE ON "semesters" WHEN (SELECT applying FROM sync_context WHERE id=1)=0 BEGIN
UPDATE sync_context SET clock=clock+1 WHERE id=1;
INSERT INTO sync_outbox(table_name,record_key,seq,payload) VALUES('semesters',json_array(OLD."id"),(SELECT clock FROM sync_context WHERE id=1),NULL) ON CONFLICT(table_name,record_key) DO UPDATE SET seq=excluded.seq,payload=excluded.payload;
END;
INSERT INTO sync_outbox SELECT 'semesters',json_array(r."id"),0,json_object('id',r."id",'name',r."name",'start_date',r."start_date",'end_date',r."end_date",'status',r."status",'created_at',r."created_at") FROM "semesters" r;
CREATE TRIGGER sync_courses_insert AFTER INSERT ON "courses" WHEN (SELECT applying FROM sync_context WHERE id=1)=0 BEGIN
UPDATE sync_context SET clock=clock+1 WHERE id=1;
INSERT INTO sync_outbox(table_name,record_key,seq,payload) VALUES('courses',json_array(NEW."id"),(SELECT clock FROM sync_context WHERE id=1),json_object('id',NEW."id",'semester_id',NEW."semester_id",'code',NEW."code",'name',NEW."name",'professor',NEW."professor",'section',NEW."section",'room',NEW."room",'credits',NEW."credits",'color',NEW."color",'archived',NEW."archived",'created_at',NEW."created_at",'updated_at',NEW."updated_at")) ON CONFLICT(table_name,record_key) DO UPDATE SET seq=excluded.seq,payload=excluded.payload;
END;
CREATE TRIGGER sync_courses_update AFTER UPDATE ON "courses" WHEN (SELECT applying FROM sync_context WHERE id=1)=0 AND (OLD."id" IS NOT NEW."id" OR OLD."semester_id" IS NOT NEW."semester_id" OR OLD."code" IS NOT NEW."code" OR OLD."name" IS NOT NEW."name" OR OLD."professor" IS NOT NEW."professor" OR OLD."section" IS NOT NEW."section" OR OLD."room" IS NOT NEW."room" OR OLD."credits" IS NOT NEW."credits" OR OLD."color" IS NOT NEW."color" OR OLD."archived" IS NOT NEW."archived" OR OLD."created_at" IS NOT NEW."created_at" OR OLD."updated_at" IS NOT NEW."updated_at") BEGIN
UPDATE sync_context SET clock=clock+1 WHERE id=1;
INSERT INTO sync_outbox(table_name,record_key,seq,payload) VALUES('courses',json_array(NEW."id"),(SELECT clock FROM sync_context WHERE id=1),json_object('id',NEW."id",'semester_id',NEW."semester_id",'code',NEW."code",'name',NEW."name",'professor',NEW."professor",'section',NEW."section",'room',NEW."room",'credits',NEW."credits",'color',NEW."color",'archived',NEW."archived",'created_at',NEW."created_at",'updated_at',NEW."updated_at")) ON CONFLICT(table_name,record_key) DO UPDATE SET seq=excluded.seq,payload=excluded.payload;
END;
CREATE TRIGGER sync_courses_delete AFTER DELETE ON "courses" WHEN (SELECT applying FROM sync_context WHERE id=1)=0 BEGIN
UPDATE sync_context SET clock=clock+1 WHERE id=1;
INSERT INTO sync_outbox(table_name,record_key,seq,payload) VALUES('courses',json_array(OLD."id"),(SELECT clock FROM sync_context WHERE id=1),NULL) ON CONFLICT(table_name,record_key) DO UPDATE SET seq=excluded.seq,payload=excluded.payload;
END;
INSERT INTO sync_outbox SELECT 'courses',json_array(r."id"),0,json_object('id',r."id",'semester_id',r."semester_id",'code',r."code",'name',r."name",'professor',r."professor",'section',r."section",'room',r."room",'credits',r."credits",'color',r."color",'archived',r."archived",'created_at',r."created_at",'updated_at',r."updated_at") FROM "courses" r;
CREATE TRIGGER sync_course_schedules_insert AFTER INSERT ON "course_schedules" WHEN (SELECT applying FROM sync_context WHERE id=1)=0 BEGIN
UPDATE sync_context SET clock=clock+1 WHERE id=1;
INSERT INTO sync_outbox(table_name,record_key,seq,payload) VALUES('course_schedules',json_array(NEW."id"),(SELECT clock FROM sync_context WHERE id=1),json_object('id',NEW."id",'course_id',NEW."course_id",'day_of_week',NEW."day_of_week",'start_time',NEW."start_time",'end_time',NEW."end_time")) ON CONFLICT(table_name,record_key) DO UPDATE SET seq=excluded.seq,payload=excluded.payload;
END;
CREATE TRIGGER sync_course_schedules_update AFTER UPDATE ON "course_schedules" WHEN (SELECT applying FROM sync_context WHERE id=1)=0 AND (OLD."id" IS NOT NEW."id" OR OLD."course_id" IS NOT NEW."course_id" OR OLD."day_of_week" IS NOT NEW."day_of_week" OR OLD."start_time" IS NOT NEW."start_time" OR OLD."end_time" IS NOT NEW."end_time") BEGIN
UPDATE sync_context SET clock=clock+1 WHERE id=1;
INSERT INTO sync_outbox(table_name,record_key,seq,payload) VALUES('course_schedules',json_array(NEW."id"),(SELECT clock FROM sync_context WHERE id=1),json_object('id',NEW."id",'course_id',NEW."course_id",'day_of_week',NEW."day_of_week",'start_time',NEW."start_time",'end_time',NEW."end_time")) ON CONFLICT(table_name,record_key) DO UPDATE SET seq=excluded.seq,payload=excluded.payload;
END;
CREATE TRIGGER sync_course_schedules_delete AFTER DELETE ON "course_schedules" WHEN (SELECT applying FROM sync_context WHERE id=1)=0 BEGIN
UPDATE sync_context SET clock=clock+1 WHERE id=1;
INSERT INTO sync_outbox(table_name,record_key,seq,payload) VALUES('course_schedules',json_array(OLD."id"),(SELECT clock FROM sync_context WHERE id=1),NULL) ON CONFLICT(table_name,record_key) DO UPDATE SET seq=excluded.seq,payload=excluded.payload;
END;
INSERT INTO sync_outbox SELECT 'course_schedules',json_array(r."id"),0,json_object('id',r."id",'course_id',r."course_id",'day_of_week',r."day_of_week",'start_time',r."start_time",'end_time',r."end_time") FROM "course_schedules" r;
CREATE TRIGGER sync_exams_insert AFTER INSERT ON "exams" WHEN (SELECT applying FROM sync_context WHERE id=1)=0 BEGIN
UPDATE sync_context SET clock=clock+1 WHERE id=1;
INSERT INTO sync_outbox(table_name,record_key,seq,payload) VALUES('exams',json_array(NEW."id"),(SELECT clock FROM sync_context WHERE id=1),json_object('id',NEW."id",'course_id',NEW."course_id",'title',NEW."title",'type',NEW."type",'date',NEW."date",'start_time',NEW."start_time",'end_time',NEW."end_time",'location',NEW."location",'description',NEW."description",'coverage',NEW."coverage",'notes',NEW."notes",'created_at',NEW."created_at",'updated_at',NEW."updated_at",'cancelled',NEW."cancelled")) ON CONFLICT(table_name,record_key) DO UPDATE SET seq=excluded.seq,payload=excluded.payload;
END;
CREATE TRIGGER sync_exams_update AFTER UPDATE ON "exams" WHEN (SELECT applying FROM sync_context WHERE id=1)=0 AND (OLD."id" IS NOT NEW."id" OR OLD."course_id" IS NOT NEW."course_id" OR OLD."title" IS NOT NEW."title" OR OLD."type" IS NOT NEW."type" OR OLD."date" IS NOT NEW."date" OR OLD."start_time" IS NOT NEW."start_time" OR OLD."end_time" IS NOT NEW."end_time" OR OLD."location" IS NOT NEW."location" OR OLD."description" IS NOT NEW."description" OR OLD."coverage" IS NOT NEW."coverage" OR OLD."notes" IS NOT NEW."notes" OR OLD."created_at" IS NOT NEW."created_at" OR OLD."updated_at" IS NOT NEW."updated_at" OR OLD."cancelled" IS NOT NEW."cancelled") BEGIN
UPDATE sync_context SET clock=clock+1 WHERE id=1;
INSERT INTO sync_outbox(table_name,record_key,seq,payload) VALUES('exams',json_array(NEW."id"),(SELECT clock FROM sync_context WHERE id=1),json_object('id',NEW."id",'course_id',NEW."course_id",'title',NEW."title",'type',NEW."type",'date',NEW."date",'start_time',NEW."start_time",'end_time',NEW."end_time",'location',NEW."location",'description',NEW."description",'coverage',NEW."coverage",'notes',NEW."notes",'created_at',NEW."created_at",'updated_at',NEW."updated_at",'cancelled',NEW."cancelled")) ON CONFLICT(table_name,record_key) DO UPDATE SET seq=excluded.seq,payload=excluded.payload;
END;
CREATE TRIGGER sync_exams_delete AFTER DELETE ON "exams" WHEN (SELECT applying FROM sync_context WHERE id=1)=0 BEGIN
UPDATE sync_context SET clock=clock+1 WHERE id=1;
INSERT INTO sync_outbox(table_name,record_key,seq,payload) VALUES('exams',json_array(OLD."id"),(SELECT clock FROM sync_context WHERE id=1),NULL) ON CONFLICT(table_name,record_key) DO UPDATE SET seq=excluded.seq,payload=excluded.payload;
END;
INSERT INTO sync_outbox SELECT 'exams',json_array(r."id"),0,json_object('id',r."id",'course_id',r."course_id",'title',r."title",'type',r."type",'date',r."date",'start_time',r."start_time",'end_time',r."end_time",'location',r."location",'description',r."description",'coverage',r."coverage",'notes',r."notes",'created_at',r."created_at",'updated_at',r."updated_at",'cancelled',r."cancelled") FROM "exams" r;
CREATE TRIGGER sync_assignments_insert AFTER INSERT ON "assignments" WHEN (SELECT applying FROM sync_context WHERE id=1)=0 BEGIN
UPDATE sync_context SET clock=clock+1 WHERE id=1;
INSERT INTO sync_outbox(table_name,record_key,seq,payload) VALUES('assignments',json_array(NEW."id"),(SELECT clock FROM sync_context WHERE id=1),json_object('id',NEW."id",'course_id',NEW."course_id",'title',NEW."title",'description',NEW."description",'assigned_date',NEW."assigned_date",'due_date',NEW."due_date",'due_time',NEW."due_time",'status',NEW."status",'notes',NEW."notes",'created_at',NEW."created_at",'updated_at',NEW."updated_at")) ON CONFLICT(table_name,record_key) DO UPDATE SET seq=excluded.seq,payload=excluded.payload;
END;
CREATE TRIGGER sync_assignments_update AFTER UPDATE ON "assignments" WHEN (SELECT applying FROM sync_context WHERE id=1)=0 AND (OLD."id" IS NOT NEW."id" OR OLD."course_id" IS NOT NEW."course_id" OR OLD."title" IS NOT NEW."title" OR OLD."description" IS NOT NEW."description" OR OLD."assigned_date" IS NOT NEW."assigned_date" OR OLD."due_date" IS NOT NEW."due_date" OR OLD."due_time" IS NOT NEW."due_time" OR OLD."status" IS NOT NEW."status" OR OLD."notes" IS NOT NEW."notes" OR OLD."created_at" IS NOT NEW."created_at" OR OLD."updated_at" IS NOT NEW."updated_at") BEGIN
UPDATE sync_context SET clock=clock+1 WHERE id=1;
INSERT INTO sync_outbox(table_name,record_key,seq,payload) VALUES('assignments',json_array(NEW."id"),(SELECT clock FROM sync_context WHERE id=1),json_object('id',NEW."id",'course_id',NEW."course_id",'title',NEW."title",'description',NEW."description",'assigned_date',NEW."assigned_date",'due_date',NEW."due_date",'due_time',NEW."due_time",'status',NEW."status",'notes',NEW."notes",'created_at',NEW."created_at",'updated_at',NEW."updated_at")) ON CONFLICT(table_name,record_key) DO UPDATE SET seq=excluded.seq,payload=excluded.payload;
END;
CREATE TRIGGER sync_assignments_delete AFTER DELETE ON "assignments" WHEN (SELECT applying FROM sync_context WHERE id=1)=0 BEGIN
UPDATE sync_context SET clock=clock+1 WHERE id=1;
INSERT INTO sync_outbox(table_name,record_key,seq,payload) VALUES('assignments',json_array(OLD."id"),(SELECT clock FROM sync_context WHERE id=1),NULL) ON CONFLICT(table_name,record_key) DO UPDATE SET seq=excluded.seq,payload=excluded.payload;
END;
INSERT INTO sync_outbox SELECT 'assignments',json_array(r."id"),0,json_object('id',r."id",'course_id',r."course_id",'title',r."title",'description',r."description",'assigned_date',r."assigned_date",'due_date',r."due_date",'due_time',r."due_time",'status',r."status",'notes',r."notes",'created_at',r."created_at",'updated_at',r."updated_at") FROM "assignments" r;
CREATE TRIGGER sync_tasks_insert AFTER INSERT ON "tasks" WHEN (SELECT applying FROM sync_context WHERE id=1)=0 BEGIN
UPDATE sync_context SET clock=clock+1 WHERE id=1;
INSERT INTO sync_outbox(table_name,record_key,seq,payload) VALUES('tasks',json_array(NEW."id"),(SELECT clock FROM sync_context WHERE id=1),json_object('id',NEW."id",'course_id',NEW."course_id",'assignment_id',NEW."assignment_id",'exam_id',NEW."exam_id",'title',NEW."title",'notes',NEW."notes",'due_date',NEW."due_date",'due_time',NEW."due_time",'priority',NEW."priority",'completed',NEW."completed",'completed_at',NEW."completed_at",'created_at',NEW."created_at",'updated_at',NEW."updated_at",'lecture_id',NEW."lecture_id")) ON CONFLICT(table_name,record_key) DO UPDATE SET seq=excluded.seq,payload=excluded.payload;
END;
CREATE TRIGGER sync_tasks_update AFTER UPDATE ON "tasks" WHEN (SELECT applying FROM sync_context WHERE id=1)=0 AND (OLD."id" IS NOT NEW."id" OR OLD."course_id" IS NOT NEW."course_id" OR OLD."assignment_id" IS NOT NEW."assignment_id" OR OLD."exam_id" IS NOT NEW."exam_id" OR OLD."title" IS NOT NEW."title" OR OLD."notes" IS NOT NEW."notes" OR OLD."due_date" IS NOT NEW."due_date" OR OLD."due_time" IS NOT NEW."due_time" OR OLD."priority" IS NOT NEW."priority" OR OLD."completed" IS NOT NEW."completed" OR OLD."completed_at" IS NOT NEW."completed_at" OR OLD."created_at" IS NOT NEW."created_at" OR OLD."updated_at" IS NOT NEW."updated_at" OR OLD."lecture_id" IS NOT NEW."lecture_id") BEGIN
UPDATE sync_context SET clock=clock+1 WHERE id=1;
INSERT INTO sync_outbox(table_name,record_key,seq,payload) VALUES('tasks',json_array(NEW."id"),(SELECT clock FROM sync_context WHERE id=1),json_object('id',NEW."id",'course_id',NEW."course_id",'assignment_id',NEW."assignment_id",'exam_id',NEW."exam_id",'title',NEW."title",'notes',NEW."notes",'due_date',NEW."due_date",'due_time',NEW."due_time",'priority',NEW."priority",'completed',NEW."completed",'completed_at',NEW."completed_at",'created_at',NEW."created_at",'updated_at',NEW."updated_at",'lecture_id',NEW."lecture_id")) ON CONFLICT(table_name,record_key) DO UPDATE SET seq=excluded.seq,payload=excluded.payload;
END;
CREATE TRIGGER sync_tasks_delete AFTER DELETE ON "tasks" WHEN (SELECT applying FROM sync_context WHERE id=1)=0 BEGIN
UPDATE sync_context SET clock=clock+1 WHERE id=1;
INSERT INTO sync_outbox(table_name,record_key,seq,payload) VALUES('tasks',json_array(OLD."id"),(SELECT clock FROM sync_context WHERE id=1),NULL) ON CONFLICT(table_name,record_key) DO UPDATE SET seq=excluded.seq,payload=excluded.payload;
END;
INSERT INTO sync_outbox SELECT 'tasks',json_array(r."id"),0,json_object('id',r."id",'course_id',r."course_id",'assignment_id',r."assignment_id",'exam_id',r."exam_id",'title',r."title",'notes',r."notes",'due_date',r."due_date",'due_time',r."due_time",'priority',r."priority",'completed',r."completed",'completed_at',r."completed_at",'created_at',r."created_at",'updated_at',r."updated_at",'lecture_id',r."lecture_id") FROM "tasks" r;
CREATE TRIGGER sync_calendar_events_insert AFTER INSERT ON "calendar_events" WHEN (SELECT applying FROM sync_context WHERE id=1)=0 BEGIN
UPDATE sync_context SET clock=clock+1 WHERE id=1;
INSERT INTO sync_outbox(table_name,record_key,seq,payload) VALUES('calendar_events',json_array(NEW."id"),(SELECT clock FROM sync_context WHERE id=1),json_object('id',NEW."id",'course_id',NEW."course_id",'title',NEW."title",'type',NEW."type",'start_datetime',NEW."start_datetime",'end_datetime',NEW."end_datetime",'all_day',NEW."all_day",'location',NEW."location",'description',NEW."description",'created_at',NEW."created_at",'updated_at',NEW."updated_at")) ON CONFLICT(table_name,record_key) DO UPDATE SET seq=excluded.seq,payload=excluded.payload;
END;
CREATE TRIGGER sync_calendar_events_update AFTER UPDATE ON "calendar_events" WHEN (SELECT applying FROM sync_context WHERE id=1)=0 AND (OLD."id" IS NOT NEW."id" OR OLD."course_id" IS NOT NEW."course_id" OR OLD."title" IS NOT NEW."title" OR OLD."type" IS NOT NEW."type" OR OLD."start_datetime" IS NOT NEW."start_datetime" OR OLD."end_datetime" IS NOT NEW."end_datetime" OR OLD."all_day" IS NOT NEW."all_day" OR OLD."location" IS NOT NEW."location" OR OLD."description" IS NOT NEW."description" OR OLD."created_at" IS NOT NEW."created_at" OR OLD."updated_at" IS NOT NEW."updated_at") BEGIN
UPDATE sync_context SET clock=clock+1 WHERE id=1;
INSERT INTO sync_outbox(table_name,record_key,seq,payload) VALUES('calendar_events',json_array(NEW."id"),(SELECT clock FROM sync_context WHERE id=1),json_object('id',NEW."id",'course_id',NEW."course_id",'title',NEW."title",'type',NEW."type",'start_datetime',NEW."start_datetime",'end_datetime',NEW."end_datetime",'all_day',NEW."all_day",'location',NEW."location",'description',NEW."description",'created_at',NEW."created_at",'updated_at',NEW."updated_at")) ON CONFLICT(table_name,record_key) DO UPDATE SET seq=excluded.seq,payload=excluded.payload;
END;
CREATE TRIGGER sync_calendar_events_delete AFTER DELETE ON "calendar_events" WHEN (SELECT applying FROM sync_context WHERE id=1)=0 BEGIN
UPDATE sync_context SET clock=clock+1 WHERE id=1;
INSERT INTO sync_outbox(table_name,record_key,seq,payload) VALUES('calendar_events',json_array(OLD."id"),(SELECT clock FROM sync_context WHERE id=1),NULL) ON CONFLICT(table_name,record_key) DO UPDATE SET seq=excluded.seq,payload=excluded.payload;
END;
INSERT INTO sync_outbox SELECT 'calendar_events',json_array(r."id"),0,json_object('id',r."id",'course_id',r."course_id",'title',r."title",'type',r."type",'start_datetime',r."start_datetime",'end_datetime',r."end_datetime",'all_day',r."all_day",'location',r."location",'description',r."description",'created_at',r."created_at",'updated_at',r."updated_at") FROM "calendar_events" r;
CREATE TRIGGER sync_files_insert AFTER INSERT ON "files" WHEN (SELECT applying FROM sync_context WHERE id=1)=0 BEGIN
UPDATE sync_context SET clock=clock+1 WHERE id=1;
INSERT INTO sync_outbox(table_name,record_key,seq,payload) VALUES('files',json_array(NEW."id"),(SELECT clock FROM sync_context WHERE id=1),json_object('id',NEW."id",'course_id',NEW."course_id",'semester_id',NEW."semester_id",'filename',NEW."filename",'original_filename',NEW."original_filename",'category',NEW."category",'extension',NEW."extension",'size',NEW."size",'created_at',NEW."created_at",'modified_at',NEW."modified_at",'added_at',NEW."added_at",'notes',NEW."notes")) ON CONFLICT(table_name,record_key) DO UPDATE SET seq=excluded.seq,payload=excluded.payload;
END;
CREATE TRIGGER sync_files_update AFTER UPDATE ON "files" WHEN (SELECT applying FROM sync_context WHERE id=1)=0 AND (OLD."id" IS NOT NEW."id" OR OLD."course_id" IS NOT NEW."course_id" OR OLD."semester_id" IS NOT NEW."semester_id" OR OLD."filename" IS NOT NEW."filename" OR OLD."original_filename" IS NOT NEW."original_filename" OR OLD."category" IS NOT NEW."category" OR OLD."extension" IS NOT NEW."extension" OR OLD."size" IS NOT NEW."size" OR OLD."created_at" IS NOT NEW."created_at" OR OLD."modified_at" IS NOT NEW."modified_at" OR OLD."added_at" IS NOT NEW."added_at" OR OLD."notes" IS NOT NEW."notes") BEGIN
UPDATE sync_context SET clock=clock+1 WHERE id=1;
INSERT INTO sync_outbox(table_name,record_key,seq,payload) VALUES('files',json_array(NEW."id"),(SELECT clock FROM sync_context WHERE id=1),json_object('id',NEW."id",'course_id',NEW."course_id",'semester_id',NEW."semester_id",'filename',NEW."filename",'original_filename',NEW."original_filename",'category',NEW."category",'extension',NEW."extension",'size',NEW."size",'created_at',NEW."created_at",'modified_at',NEW."modified_at",'added_at',NEW."added_at",'notes',NEW."notes")) ON CONFLICT(table_name,record_key) DO UPDATE SET seq=excluded.seq,payload=excluded.payload;
END;
CREATE TRIGGER sync_files_delete AFTER DELETE ON "files" WHEN (SELECT applying FROM sync_context WHERE id=1)=0 BEGIN
UPDATE sync_context SET clock=clock+1 WHERE id=1;
INSERT INTO sync_outbox(table_name,record_key,seq,payload) VALUES('files',json_array(OLD."id"),(SELECT clock FROM sync_context WHERE id=1),NULL) ON CONFLICT(table_name,record_key) DO UPDATE SET seq=excluded.seq,payload=excluded.payload;
END;
INSERT INTO sync_outbox SELECT 'files',json_array(r."id"),0,json_object('id',r."id",'course_id',r."course_id",'semester_id',r."semester_id",'filename',r."filename",'original_filename',r."original_filename",'category',r."category",'extension',r."extension",'size',r."size",'created_at',r."created_at",'modified_at',r."modified_at",'added_at',r."added_at",'notes',r."notes") FROM "files" r;
CREATE TRIGGER sync_attachments_insert AFTER INSERT ON "attachments" WHEN (SELECT applying FROM sync_context WHERE id=1)=0 BEGIN
UPDATE sync_context SET clock=clock+1 WHERE id=1;
INSERT INTO sync_outbox(table_name,record_key,seq,payload) VALUES('attachments',json_array(NEW."file_id",NEW."entity_id",NEW."entity_type"),(SELECT clock FROM sync_context WHERE id=1),json_object('file_id',NEW."file_id",'entity_id',NEW."entity_id",'entity_type',NEW."entity_type")) ON CONFLICT(table_name,record_key) DO UPDATE SET seq=excluded.seq,payload=excluded.payload;
END;
CREATE TRIGGER sync_attachments_update AFTER UPDATE ON "attachments" WHEN (SELECT applying FROM sync_context WHERE id=1)=0 AND (OLD."file_id" IS NOT NEW."file_id" OR OLD."entity_id" IS NOT NEW."entity_id" OR OLD."entity_type" IS NOT NEW."entity_type") BEGIN
UPDATE sync_context SET clock=clock+1 WHERE id=1;
INSERT INTO sync_outbox(table_name,record_key,seq,payload) VALUES('attachments',json_array(NEW."file_id",NEW."entity_id",NEW."entity_type"),(SELECT clock FROM sync_context WHERE id=1),json_object('file_id',NEW."file_id",'entity_id',NEW."entity_id",'entity_type',NEW."entity_type")) ON CONFLICT(table_name,record_key) DO UPDATE SET seq=excluded.seq,payload=excluded.payload;
END;
CREATE TRIGGER sync_attachments_delete AFTER DELETE ON "attachments" WHEN (SELECT applying FROM sync_context WHERE id=1)=0 BEGIN
UPDATE sync_context SET clock=clock+1 WHERE id=1;
INSERT INTO sync_outbox(table_name,record_key,seq,payload) VALUES('attachments',json_array(OLD."file_id",OLD."entity_id",OLD."entity_type"),(SELECT clock FROM sync_context WHERE id=1),NULL) ON CONFLICT(table_name,record_key) DO UPDATE SET seq=excluded.seq,payload=excluded.payload;
END;
INSERT INTO sync_outbox SELECT 'attachments',json_array(r."file_id",r."entity_id",r."entity_type"),0,json_object('file_id',r."file_id",'entity_id',r."entity_id",'entity_type',r."entity_type") FROM "attachments" r;
CREATE TRIGGER sync_course_schedule_exceptions_insert AFTER INSERT ON "course_schedule_exceptions" WHEN (SELECT applying FROM sync_context WHERE id=1)=0 BEGIN
UPDATE sync_context SET clock=clock+1 WHERE id=1;
INSERT INTO sync_outbox(table_name,record_key,seq,payload) VALUES('course_schedule_exceptions',json_array(NEW."id"),(SELECT clock FROM sync_context WHERE id=1),json_object('id',NEW."id",'course_id',NEW."course_id",'date',NEW."date",'exception_type',NEW."exception_type",'original_start_time',NEW."original_start_time",'original_end_time',NEW."original_end_time",'new_date',NEW."new_date",'new_start_time',NEW."new_start_time",'new_end_time',NEW."new_end_time",'room',NEW."room",'reason',NEW."reason",'created_at',NEW."created_at",'updated_at',NEW."updated_at")) ON CONFLICT(table_name,record_key) DO UPDATE SET seq=excluded.seq,payload=excluded.payload;
END;
CREATE TRIGGER sync_course_schedule_exceptions_update AFTER UPDATE ON "course_schedule_exceptions" WHEN (SELECT applying FROM sync_context WHERE id=1)=0 AND (OLD."id" IS NOT NEW."id" OR OLD."course_id" IS NOT NEW."course_id" OR OLD."date" IS NOT NEW."date" OR OLD."exception_type" IS NOT NEW."exception_type" OR OLD."original_start_time" IS NOT NEW."original_start_time" OR OLD."original_end_time" IS NOT NEW."original_end_time" OR OLD."new_date" IS NOT NEW."new_date" OR OLD."new_start_time" IS NOT NEW."new_start_time" OR OLD."new_end_time" IS NOT NEW."new_end_time" OR OLD."room" IS NOT NEW."room" OR OLD."reason" IS NOT NEW."reason" OR OLD."created_at" IS NOT NEW."created_at" OR OLD."updated_at" IS NOT NEW."updated_at") BEGIN
UPDATE sync_context SET clock=clock+1 WHERE id=1;
INSERT INTO sync_outbox(table_name,record_key,seq,payload) VALUES('course_schedule_exceptions',json_array(NEW."id"),(SELECT clock FROM sync_context WHERE id=1),json_object('id',NEW."id",'course_id',NEW."course_id",'date',NEW."date",'exception_type',NEW."exception_type",'original_start_time',NEW."original_start_time",'original_end_time',NEW."original_end_time",'new_date',NEW."new_date",'new_start_time',NEW."new_start_time",'new_end_time',NEW."new_end_time",'room',NEW."room",'reason',NEW."reason",'created_at',NEW."created_at",'updated_at',NEW."updated_at")) ON CONFLICT(table_name,record_key) DO UPDATE SET seq=excluded.seq,payload=excluded.payload;
END;
CREATE TRIGGER sync_course_schedule_exceptions_delete AFTER DELETE ON "course_schedule_exceptions" WHEN (SELECT applying FROM sync_context WHERE id=1)=0 BEGIN
UPDATE sync_context SET clock=clock+1 WHERE id=1;
INSERT INTO sync_outbox(table_name,record_key,seq,payload) VALUES('course_schedule_exceptions',json_array(OLD."id"),(SELECT clock FROM sync_context WHERE id=1),NULL) ON CONFLICT(table_name,record_key) DO UPDATE SET seq=excluded.seq,payload=excluded.payload;
END;
INSERT INTO sync_outbox SELECT 'course_schedule_exceptions',json_array(r."id"),0,json_object('id',r."id",'course_id',r."course_id",'date',r."date",'exception_type',r."exception_type",'original_start_time',r."original_start_time",'original_end_time',r."original_end_time",'new_date',r."new_date",'new_start_time',r."new_start_time",'new_end_time',r."new_end_time",'room',r."room",'reason',r."reason",'created_at',r."created_at",'updated_at',r."updated_at") FROM "course_schedule_exceptions" r;
CREATE TRIGGER sync_academic_change_log_insert AFTER INSERT ON "academic_change_log" WHEN (SELECT applying FROM sync_context WHERE id=1)=0 BEGIN
UPDATE sync_context SET clock=clock+1 WHERE id=1;
INSERT INTO sync_outbox(table_name,record_key,seq,payload) VALUES('academic_change_log',json_array(NEW."id"),(SELECT clock FROM sync_context WHERE id=1),json_object('id',NEW."id",'entity_type',NEW."entity_type",'entity_id',NEW."entity_id",'field_name',NEW."field_name",'old_value',NEW."old_value",'new_value',NEW."new_value",'source_type',NEW."source_type",'source_sender',NEW."source_sender",'source_subject',NEW."source_subject",'source_received_at',NEW."source_received_at",'created_at',NEW."created_at")) ON CONFLICT(table_name,record_key) DO UPDATE SET seq=excluded.seq,payload=excluded.payload;
END;
CREATE TRIGGER sync_academic_change_log_update AFTER UPDATE ON "academic_change_log" WHEN (SELECT applying FROM sync_context WHERE id=1)=0 AND (OLD."id" IS NOT NEW."id" OR OLD."entity_type" IS NOT NEW."entity_type" OR OLD."entity_id" IS NOT NEW."entity_id" OR OLD."field_name" IS NOT NEW."field_name" OR OLD."old_value" IS NOT NEW."old_value" OR OLD."new_value" IS NOT NEW."new_value" OR OLD."source_type" IS NOT NEW."source_type" OR OLD."source_sender" IS NOT NEW."source_sender" OR OLD."source_subject" IS NOT NEW."source_subject" OR OLD."source_received_at" IS NOT NEW."source_received_at" OR OLD."created_at" IS NOT NEW."created_at") BEGIN
UPDATE sync_context SET clock=clock+1 WHERE id=1;
INSERT INTO sync_outbox(table_name,record_key,seq,payload) VALUES('academic_change_log',json_array(NEW."id"),(SELECT clock FROM sync_context WHERE id=1),json_object('id',NEW."id",'entity_type',NEW."entity_type",'entity_id',NEW."entity_id",'field_name',NEW."field_name",'old_value',NEW."old_value",'new_value',NEW."new_value",'source_type',NEW."source_type",'source_sender',NEW."source_sender",'source_subject',NEW."source_subject",'source_received_at',NEW."source_received_at",'created_at',NEW."created_at")) ON CONFLICT(table_name,record_key) DO UPDATE SET seq=excluded.seq,payload=excluded.payload;
END;
CREATE TRIGGER sync_academic_change_log_delete AFTER DELETE ON "academic_change_log" WHEN (SELECT applying FROM sync_context WHERE id=1)=0 BEGIN
UPDATE sync_context SET clock=clock+1 WHERE id=1;
INSERT INTO sync_outbox(table_name,record_key,seq,payload) VALUES('academic_change_log',json_array(OLD."id"),(SELECT clock FROM sync_context WHERE id=1),NULL) ON CONFLICT(table_name,record_key) DO UPDATE SET seq=excluded.seq,payload=excluded.payload;
END;
INSERT INTO sync_outbox SELECT 'academic_change_log',json_array(r."id"),0,json_object('id',r."id",'entity_type',r."entity_type",'entity_id',r."entity_id",'field_name',r."field_name",'old_value',r."old_value",'new_value',r."new_value",'source_type',r."source_type",'source_sender',r."source_sender",'source_subject',r."source_subject",'source_received_at',r."source_received_at",'created_at',r."created_at") FROM "academic_change_log" r;
CREATE TRIGGER sync_lectures_insert AFTER INSERT ON "lectures" WHEN (SELECT applying FROM sync_context WHERE id=1)=0 BEGIN
UPDATE sync_context SET clock=clock+1 WHERE id=1;
INSERT INTO sync_outbox(table_name,record_key,seq,payload) VALUES('lectures',json_array(NEW."id"),(SELECT clock FROM sync_context WHERE id=1),json_object('id',NEW."id",'course_id',NEW."course_id",'number',NEW."number",'title',NEW."title",'lecture_date',NEW."lecture_date",'status',NEW."status",'reviewed_at',NEW."reviewed_at",'confidence',NEW."confidence",'notes',NEW."notes",'estimated_minutes',NEW."estimated_minutes",'created_at',NEW."created_at",'updated_at',NEW."updated_at")) ON CONFLICT(table_name,record_key) DO UPDATE SET seq=excluded.seq,payload=excluded.payload;
END;
CREATE TRIGGER sync_lectures_update AFTER UPDATE ON "lectures" WHEN (SELECT applying FROM sync_context WHERE id=1)=0 AND (OLD."id" IS NOT NEW."id" OR OLD."course_id" IS NOT NEW."course_id" OR OLD."number" IS NOT NEW."number" OR OLD."title" IS NOT NEW."title" OR OLD."lecture_date" IS NOT NEW."lecture_date" OR OLD."status" IS NOT NEW."status" OR OLD."reviewed_at" IS NOT NEW."reviewed_at" OR OLD."confidence" IS NOT NEW."confidence" OR OLD."notes" IS NOT NEW."notes" OR OLD."estimated_minutes" IS NOT NEW."estimated_minutes" OR OLD."created_at" IS NOT NEW."created_at" OR OLD."updated_at" IS NOT NEW."updated_at") BEGIN
UPDATE sync_context SET clock=clock+1 WHERE id=1;
INSERT INTO sync_outbox(table_name,record_key,seq,payload) VALUES('lectures',json_array(NEW."id"),(SELECT clock FROM sync_context WHERE id=1),json_object('id',NEW."id",'course_id',NEW."course_id",'number',NEW."number",'title',NEW."title",'lecture_date',NEW."lecture_date",'status',NEW."status",'reviewed_at',NEW."reviewed_at",'confidence',NEW."confidence",'notes',NEW."notes",'estimated_minutes',NEW."estimated_minutes",'created_at',NEW."created_at",'updated_at',NEW."updated_at")) ON CONFLICT(table_name,record_key) DO UPDATE SET seq=excluded.seq,payload=excluded.payload;
END;
CREATE TRIGGER sync_lectures_delete AFTER DELETE ON "lectures" WHEN (SELECT applying FROM sync_context WHERE id=1)=0 BEGIN
UPDATE sync_context SET clock=clock+1 WHERE id=1;
INSERT INTO sync_outbox(table_name,record_key,seq,payload) VALUES('lectures',json_array(OLD."id"),(SELECT clock FROM sync_context WHERE id=1),NULL) ON CONFLICT(table_name,record_key) DO UPDATE SET seq=excluded.seq,payload=excluded.payload;
END;
INSERT INTO sync_outbox SELECT 'lectures',json_array(r."id"),0,json_object('id',r."id",'course_id',r."course_id",'number',r."number",'title',r."title",'lecture_date',r."lecture_date",'status',r."status",'reviewed_at',r."reviewed_at",'confidence',r."confidence",'notes',r."notes",'estimated_minutes',r."estimated_minutes",'created_at',r."created_at",'updated_at',r."updated_at") FROM "lectures" r;
CREATE TRIGGER sync_readings_insert AFTER INSERT ON "readings" WHEN (SELECT applying FROM sync_context WHERE id=1)=0 BEGIN
UPDATE sync_context SET clock=clock+1 WHERE id=1;
INSERT INTO sync_outbox(table_name,record_key,seq,payload) VALUES('readings',json_array(NEW."id"),(SELECT clock FROM sync_context WHERE id=1),json_object('id',NEW."id",'course_id',NEW."course_id",'title',NEW."title",'author',NEW."author",'assigned_date',NEW."assigned_date",'due_date',NEW."due_date",'status',NEW."status",'file_id',NEW."file_id",'external_reference',NEW."external_reference",'pages',NEW."pages",'notes',NEW."notes",'created_at',NEW."created_at",'updated_at',NEW."updated_at")) ON CONFLICT(table_name,record_key) DO UPDATE SET seq=excluded.seq,payload=excluded.payload;
END;
CREATE TRIGGER sync_readings_update AFTER UPDATE ON "readings" WHEN (SELECT applying FROM sync_context WHERE id=1)=0 AND (OLD."id" IS NOT NEW."id" OR OLD."course_id" IS NOT NEW."course_id" OR OLD."title" IS NOT NEW."title" OR OLD."author" IS NOT NEW."author" OR OLD."assigned_date" IS NOT NEW."assigned_date" OR OLD."due_date" IS NOT NEW."due_date" OR OLD."status" IS NOT NEW."status" OR OLD."file_id" IS NOT NEW."file_id" OR OLD."external_reference" IS NOT NEW."external_reference" OR OLD."pages" IS NOT NEW."pages" OR OLD."notes" IS NOT NEW."notes" OR OLD."created_at" IS NOT NEW."created_at" OR OLD."updated_at" IS NOT NEW."updated_at") BEGIN
UPDATE sync_context SET clock=clock+1 WHERE id=1;
INSERT INTO sync_outbox(table_name,record_key,seq,payload) VALUES('readings',json_array(NEW."id"),(SELECT clock FROM sync_context WHERE id=1),json_object('id',NEW."id",'course_id',NEW."course_id",'title',NEW."title",'author',NEW."author",'assigned_date',NEW."assigned_date",'due_date',NEW."due_date",'status',NEW."status",'file_id',NEW."file_id",'external_reference',NEW."external_reference",'pages',NEW."pages",'notes',NEW."notes",'created_at',NEW."created_at",'updated_at',NEW."updated_at")) ON CONFLICT(table_name,record_key) DO UPDATE SET seq=excluded.seq,payload=excluded.payload;
END;
CREATE TRIGGER sync_readings_delete AFTER DELETE ON "readings" WHEN (SELECT applying FROM sync_context WHERE id=1)=0 BEGIN
UPDATE sync_context SET clock=clock+1 WHERE id=1;
INSERT INTO sync_outbox(table_name,record_key,seq,payload) VALUES('readings',json_array(OLD."id"),(SELECT clock FROM sync_context WHERE id=1),NULL) ON CONFLICT(table_name,record_key) DO UPDATE SET seq=excluded.seq,payload=excluded.payload;
END;
INSERT INTO sync_outbox SELECT 'readings',json_array(r."id"),0,json_object('id',r."id",'course_id',r."course_id",'title',r."title",'author',r."author",'assigned_date',r."assigned_date",'due_date',r."due_date",'status',r."status",'file_id',r."file_id",'external_reference',r."external_reference",'pages',r."pages",'notes',r."notes",'created_at',r."created_at",'updated_at',r."updated_at") FROM "readings" r;
CREATE TRIGGER sync_grade_categories_insert AFTER INSERT ON "grade_categories" WHEN (SELECT applying FROM sync_context WHERE id=1)=0 BEGIN
UPDATE sync_context SET clock=clock+1 WHERE id=1;
INSERT INTO sync_outbox(table_name,record_key,seq,payload) VALUES('grade_categories',json_array(NEW."id"),(SELECT clock FROM sync_context WHERE id=1),json_object('id',NEW."id",'course_id',NEW."course_id",'name',NEW."name",'weight',NEW."weight",'sort_order',NEW."sort_order",'created_at',NEW."created_at",'updated_at',NEW."updated_at")) ON CONFLICT(table_name,record_key) DO UPDATE SET seq=excluded.seq,payload=excluded.payload;
END;
CREATE TRIGGER sync_grade_categories_update AFTER UPDATE ON "grade_categories" WHEN (SELECT applying FROM sync_context WHERE id=1)=0 AND (OLD."id" IS NOT NEW."id" OR OLD."course_id" IS NOT NEW."course_id" OR OLD."name" IS NOT NEW."name" OR OLD."weight" IS NOT NEW."weight" OR OLD."sort_order" IS NOT NEW."sort_order" OR OLD."created_at" IS NOT NEW."created_at" OR OLD."updated_at" IS NOT NEW."updated_at") BEGIN
UPDATE sync_context SET clock=clock+1 WHERE id=1;
INSERT INTO sync_outbox(table_name,record_key,seq,payload) VALUES('grade_categories',json_array(NEW."id"),(SELECT clock FROM sync_context WHERE id=1),json_object('id',NEW."id",'course_id',NEW."course_id",'name',NEW."name",'weight',NEW."weight",'sort_order',NEW."sort_order",'created_at',NEW."created_at",'updated_at',NEW."updated_at")) ON CONFLICT(table_name,record_key) DO UPDATE SET seq=excluded.seq,payload=excluded.payload;
END;
CREATE TRIGGER sync_grade_categories_delete AFTER DELETE ON "grade_categories" WHEN (SELECT applying FROM sync_context WHERE id=1)=0 BEGIN
UPDATE sync_context SET clock=clock+1 WHERE id=1;
INSERT INTO sync_outbox(table_name,record_key,seq,payload) VALUES('grade_categories',json_array(OLD."id"),(SELECT clock FROM sync_context WHERE id=1),NULL) ON CONFLICT(table_name,record_key) DO UPDATE SET seq=excluded.seq,payload=excluded.payload;
END;
INSERT INTO sync_outbox SELECT 'grade_categories',json_array(r."id"),0,json_object('id',r."id",'course_id',r."course_id",'name',r."name",'weight',r."weight",'sort_order',r."sort_order",'created_at',r."created_at",'updated_at',r."updated_at") FROM "grade_categories" r;
CREATE TRIGGER sync_grade_items_insert AFTER INSERT ON "grade_items" WHEN (SELECT applying FROM sync_context WHERE id=1)=0 BEGIN
UPDATE sync_context SET clock=clock+1 WHERE id=1;
INSERT INTO sync_outbox(table_name,record_key,seq,payload) VALUES('grade_items',json_array(NEW."id"),(SELECT clock FROM sync_context WHERE id=1),json_object('id',NEW."id",'course_id',NEW."course_id",'category_id',NEW."category_id",'assignment_id',NEW."assignment_id",'exam_id',NEW."exam_id",'title',NEW."title",'points_earned',NEW."points_earned",'points_possible',NEW."points_possible",'weight_override',NEW."weight_override",'excluded',NEW."excluded",'assessment_date',NEW."assessment_date",'notes',NEW."notes",'created_at',NEW."created_at",'updated_at',NEW."updated_at")) ON CONFLICT(table_name,record_key) DO UPDATE SET seq=excluded.seq,payload=excluded.payload;
END;
CREATE TRIGGER sync_grade_items_update AFTER UPDATE ON "grade_items" WHEN (SELECT applying FROM sync_context WHERE id=1)=0 AND (OLD."id" IS NOT NEW."id" OR OLD."course_id" IS NOT NEW."course_id" OR OLD."category_id" IS NOT NEW."category_id" OR OLD."assignment_id" IS NOT NEW."assignment_id" OR OLD."exam_id" IS NOT NEW."exam_id" OR OLD."title" IS NOT NEW."title" OR OLD."points_earned" IS NOT NEW."points_earned" OR OLD."points_possible" IS NOT NEW."points_possible" OR OLD."weight_override" IS NOT NEW."weight_override" OR OLD."excluded" IS NOT NEW."excluded" OR OLD."assessment_date" IS NOT NEW."assessment_date" OR OLD."notes" IS NOT NEW."notes" OR OLD."created_at" IS NOT NEW."created_at" OR OLD."updated_at" IS NOT NEW."updated_at") BEGIN
UPDATE sync_context SET clock=clock+1 WHERE id=1;
INSERT INTO sync_outbox(table_name,record_key,seq,payload) VALUES('grade_items',json_array(NEW."id"),(SELECT clock FROM sync_context WHERE id=1),json_object('id',NEW."id",'course_id',NEW."course_id",'category_id',NEW."category_id",'assignment_id',NEW."assignment_id",'exam_id',NEW."exam_id",'title',NEW."title",'points_earned',NEW."points_earned",'points_possible',NEW."points_possible",'weight_override',NEW."weight_override",'excluded',NEW."excluded",'assessment_date',NEW."assessment_date",'notes',NEW."notes",'created_at',NEW."created_at",'updated_at',NEW."updated_at")) ON CONFLICT(table_name,record_key) DO UPDATE SET seq=excluded.seq,payload=excluded.payload;
END;
CREATE TRIGGER sync_grade_items_delete AFTER DELETE ON "grade_items" WHEN (SELECT applying FROM sync_context WHERE id=1)=0 BEGIN
UPDATE sync_context SET clock=clock+1 WHERE id=1;
INSERT INTO sync_outbox(table_name,record_key,seq,payload) VALUES('grade_items',json_array(OLD."id"),(SELECT clock FROM sync_context WHERE id=1),NULL) ON CONFLICT(table_name,record_key) DO UPDATE SET seq=excluded.seq,payload=excluded.payload;
END;
INSERT INTO sync_outbox SELECT 'grade_items',json_array(r."id"),0,json_object('id',r."id",'course_id',r."course_id",'category_id',r."category_id",'assignment_id',r."assignment_id",'exam_id',r."exam_id",'title',r."title",'points_earned',r."points_earned",'points_possible',r."points_possible",'weight_override',r."weight_override",'excluded',r."excluded",'assessment_date',r."assessment_date",'notes',r."notes",'created_at',r."created_at",'updated_at',r."updated_at") FROM "grade_items" r;
CREATE TRIGGER sync_previous_exam_attempts_insert AFTER INSERT ON "previous_exam_attempts" WHEN (SELECT applying FROM sync_context WHERE id=1)=0 BEGIN
UPDATE sync_context SET clock=clock+1 WHERE id=1;
INSERT INTO sync_outbox(table_name,record_key,seq,payload) VALUES('previous_exam_attempts',json_array(NEW."id"),(SELECT clock FROM sync_context WHERE id=1),json_object('id',NEW."id",'course_id',NEW."course_id",'file_id',NEW."file_id",'title',NEW."title",'status',NEW."status",'completed_at',NEW."completed_at",'score',NEW."score",'notes',NEW."notes",'created_at',NEW."created_at",'updated_at',NEW."updated_at")) ON CONFLICT(table_name,record_key) DO UPDATE SET seq=excluded.seq,payload=excluded.payload;
END;
CREATE TRIGGER sync_previous_exam_attempts_update AFTER UPDATE ON "previous_exam_attempts" WHEN (SELECT applying FROM sync_context WHERE id=1)=0 AND (OLD."id" IS NOT NEW."id" OR OLD."course_id" IS NOT NEW."course_id" OR OLD."file_id" IS NOT NEW."file_id" OR OLD."title" IS NOT NEW."title" OR OLD."status" IS NOT NEW."status" OR OLD."completed_at" IS NOT NEW."completed_at" OR OLD."score" IS NOT NEW."score" OR OLD."notes" IS NOT NEW."notes" OR OLD."created_at" IS NOT NEW."created_at" OR OLD."updated_at" IS NOT NEW."updated_at") BEGIN
UPDATE sync_context SET clock=clock+1 WHERE id=1;
INSERT INTO sync_outbox(table_name,record_key,seq,payload) VALUES('previous_exam_attempts',json_array(NEW."id"),(SELECT clock FROM sync_context WHERE id=1),json_object('id',NEW."id",'course_id',NEW."course_id",'file_id',NEW."file_id",'title',NEW."title",'status',NEW."status",'completed_at',NEW."completed_at",'score',NEW."score",'notes',NEW."notes",'created_at',NEW."created_at",'updated_at',NEW."updated_at")) ON CONFLICT(table_name,record_key) DO UPDATE SET seq=excluded.seq,payload=excluded.payload;
END;
CREATE TRIGGER sync_previous_exam_attempts_delete AFTER DELETE ON "previous_exam_attempts" WHEN (SELECT applying FROM sync_context WHERE id=1)=0 BEGIN
UPDATE sync_context SET clock=clock+1 WHERE id=1;
INSERT INTO sync_outbox(table_name,record_key,seq,payload) VALUES('previous_exam_attempts',json_array(OLD."id"),(SELECT clock FROM sync_context WHERE id=1),NULL) ON CONFLICT(table_name,record_key) DO UPDATE SET seq=excluded.seq,payload=excluded.payload;
END;
INSERT INTO sync_outbox SELECT 'previous_exam_attempts',json_array(r."id"),0,json_object('id',r."id",'course_id',r."course_id",'file_id',r."file_id",'title',r."title",'status',r."status",'completed_at',r."completed_at",'score',r."score",'notes',r."notes",'created_at',r."created_at",'updated_at',r."updated_at") FROM "previous_exam_attempts" r;
CREATE TRIGGER sync_exam_topics_insert AFTER INSERT ON "exam_topics" WHEN (SELECT applying FROM sync_context WHERE id=1)=0 BEGIN
UPDATE sync_context SET clock=clock+1 WHERE id=1;
INSERT INTO sync_outbox(table_name,record_key,seq,payload) VALUES('exam_topics',json_array(NEW."id"),(SELECT clock FROM sync_context WHERE id=1),json_object('id',NEW."id",'course_id',NEW."course_id",'exam_id',NEW."exam_id",'lecture_id',NEW."lecture_id",'reading_id',NEW."reading_id",'previous_exam_id',NEW."previous_exam_id",'title',NEW."title",'status',NEW."status",'confidence',NEW."confidence",'notes',NEW."notes",'sort_order',NEW."sort_order",'created_at',NEW."created_at",'updated_at',NEW."updated_at")) ON CONFLICT(table_name,record_key) DO UPDATE SET seq=excluded.seq,payload=excluded.payload;
END;
CREATE TRIGGER sync_exam_topics_update AFTER UPDATE ON "exam_topics" WHEN (SELECT applying FROM sync_context WHERE id=1)=0 AND (OLD."id" IS NOT NEW."id" OR OLD."course_id" IS NOT NEW."course_id" OR OLD."exam_id" IS NOT NEW."exam_id" OR OLD."lecture_id" IS NOT NEW."lecture_id" OR OLD."reading_id" IS NOT NEW."reading_id" OR OLD."previous_exam_id" IS NOT NEW."previous_exam_id" OR OLD."title" IS NOT NEW."title" OR OLD."status" IS NOT NEW."status" OR OLD."confidence" IS NOT NEW."confidence" OR OLD."notes" IS NOT NEW."notes" OR OLD."sort_order" IS NOT NEW."sort_order" OR OLD."created_at" IS NOT NEW."created_at" OR OLD."updated_at" IS NOT NEW."updated_at") BEGIN
UPDATE sync_context SET clock=clock+1 WHERE id=1;
INSERT INTO sync_outbox(table_name,record_key,seq,payload) VALUES('exam_topics',json_array(NEW."id"),(SELECT clock FROM sync_context WHERE id=1),json_object('id',NEW."id",'course_id',NEW."course_id",'exam_id',NEW."exam_id",'lecture_id',NEW."lecture_id",'reading_id',NEW."reading_id",'previous_exam_id',NEW."previous_exam_id",'title',NEW."title",'status',NEW."status",'confidence',NEW."confidence",'notes',NEW."notes",'sort_order',NEW."sort_order",'created_at',NEW."created_at",'updated_at',NEW."updated_at")) ON CONFLICT(table_name,record_key) DO UPDATE SET seq=excluded.seq,payload=excluded.payload;
END;
CREATE TRIGGER sync_exam_topics_delete AFTER DELETE ON "exam_topics" WHEN (SELECT applying FROM sync_context WHERE id=1)=0 BEGIN
UPDATE sync_context SET clock=clock+1 WHERE id=1;
INSERT INTO sync_outbox(table_name,record_key,seq,payload) VALUES('exam_topics',json_array(OLD."id"),(SELECT clock FROM sync_context WHERE id=1),NULL) ON CONFLICT(table_name,record_key) DO UPDATE SET seq=excluded.seq,payload=excluded.payload;
END;
INSERT INTO sync_outbox SELECT 'exam_topics',json_array(r."id"),0,json_object('id',r."id",'course_id',r."course_id",'exam_id',r."exam_id",'lecture_id',r."lecture_id",'reading_id',r."reading_id",'previous_exam_id',r."previous_exam_id",'title',r."title",'status',r."status",'confidence',r."confidence",'notes',r."notes",'sort_order',r."sort_order",'created_at',r."created_at",'updated_at',r."updated_at") FROM "exam_topics" r;
CREATE TRIGGER sync_course_topics_insert AFTER INSERT ON "course_topics" WHEN (SELECT applying FROM sync_context WHERE id=1)=0 BEGIN
UPDATE sync_context SET clock=clock+1 WHERE id=1;
INSERT INTO sync_outbox(table_name,record_key,seq,payload) VALUES('course_topics',json_array(NEW."id"),(SELECT clock FROM sync_context WHERE id=1),json_object('id',NEW."id",'course_id',NEW."course_id",'title',NEW."title",'status',NEW."status",'notes',NEW."notes",'created_at',NEW."created_at",'updated_at',NEW."updated_at")) ON CONFLICT(table_name,record_key) DO UPDATE SET seq=excluded.seq,payload=excluded.payload;
END;
CREATE TRIGGER sync_course_topics_update AFTER UPDATE ON "course_topics" WHEN (SELECT applying FROM sync_context WHERE id=1)=0 AND (OLD."id" IS NOT NEW."id" OR OLD."course_id" IS NOT NEW."course_id" OR OLD."title" IS NOT NEW."title" OR OLD."status" IS NOT NEW."status" OR OLD."notes" IS NOT NEW."notes" OR OLD."created_at" IS NOT NEW."created_at" OR OLD."updated_at" IS NOT NEW."updated_at") BEGIN
UPDATE sync_context SET clock=clock+1 WHERE id=1;
INSERT INTO sync_outbox(table_name,record_key,seq,payload) VALUES('course_topics',json_array(NEW."id"),(SELECT clock FROM sync_context WHERE id=1),json_object('id',NEW."id",'course_id',NEW."course_id",'title',NEW."title",'status',NEW."status",'notes',NEW."notes",'created_at',NEW."created_at",'updated_at',NEW."updated_at")) ON CONFLICT(table_name,record_key) DO UPDATE SET seq=excluded.seq,payload=excluded.payload;
END;
CREATE TRIGGER sync_course_topics_delete AFTER DELETE ON "course_topics" WHEN (SELECT applying FROM sync_context WHERE id=1)=0 BEGIN
UPDATE sync_context SET clock=clock+1 WHERE id=1;
INSERT INTO sync_outbox(table_name,record_key,seq,payload) VALUES('course_topics',json_array(OLD."id"),(SELECT clock FROM sync_context WHERE id=1),NULL) ON CONFLICT(table_name,record_key) DO UPDATE SET seq=excluded.seq,payload=excluded.payload;
END;
INSERT INTO sync_outbox SELECT 'course_topics',json_array(r."id"),0,json_object('id',r."id",'course_id',r."course_id",'title',r."title",'status',r."status",'notes',r."notes",'created_at',r."created_at",'updated_at',r."updated_at") FROM "course_topics" r;
CREATE TRIGGER sync_study_sessions_insert AFTER INSERT ON "study_sessions" WHEN (SELECT applying FROM sync_context WHERE id=1)=0 BEGIN
UPDATE sync_context SET clock=clock+1 WHERE id=1;
INSERT INTO sync_outbox(table_name,record_key,seq,payload) VALUES('study_sessions',json_array(NEW."id"),(SELECT clock FROM sync_context WHERE id=1),json_object('id',NEW."id",'course_id',NEW."course_id",'exam_id',NEW."exam_id",'lecture_id',NEW."lecture_id",'reading_id',NEW."reading_id",'task_id',NEW."task_id",'title',NEW."title",'started_at',NEW."started_at",'ended_at',NEW."ended_at",'duration_seconds',NEW."duration_seconds",'notes',NEW."notes",'created_at',NEW."created_at",'updated_at',NEW."updated_at")) ON CONFLICT(table_name,record_key) DO UPDATE SET seq=excluded.seq,payload=excluded.payload;
END;
CREATE TRIGGER sync_study_sessions_update AFTER UPDATE ON "study_sessions" WHEN (SELECT applying FROM sync_context WHERE id=1)=0 AND (OLD."id" IS NOT NEW."id" OR OLD."course_id" IS NOT NEW."course_id" OR OLD."exam_id" IS NOT NEW."exam_id" OR OLD."lecture_id" IS NOT NEW."lecture_id" OR OLD."reading_id" IS NOT NEW."reading_id" OR OLD."task_id" IS NOT NEW."task_id" OR OLD."title" IS NOT NEW."title" OR OLD."started_at" IS NOT NEW."started_at" OR OLD."ended_at" IS NOT NEW."ended_at" OR OLD."duration_seconds" IS NOT NEW."duration_seconds" OR OLD."notes" IS NOT NEW."notes" OR OLD."created_at" IS NOT NEW."created_at" OR OLD."updated_at" IS NOT NEW."updated_at") BEGIN
UPDATE sync_context SET clock=clock+1 WHERE id=1;
INSERT INTO sync_outbox(table_name,record_key,seq,payload) VALUES('study_sessions',json_array(NEW."id"),(SELECT clock FROM sync_context WHERE id=1),json_object('id',NEW."id",'course_id',NEW."course_id",'exam_id',NEW."exam_id",'lecture_id',NEW."lecture_id",'reading_id',NEW."reading_id",'task_id',NEW."task_id",'title',NEW."title",'started_at',NEW."started_at",'ended_at',NEW."ended_at",'duration_seconds',NEW."duration_seconds",'notes',NEW."notes",'created_at',NEW."created_at",'updated_at',NEW."updated_at")) ON CONFLICT(table_name,record_key) DO UPDATE SET seq=excluded.seq,payload=excluded.payload;
END;
CREATE TRIGGER sync_study_sessions_delete AFTER DELETE ON "study_sessions" WHEN (SELECT applying FROM sync_context WHERE id=1)=0 BEGIN
UPDATE sync_context SET clock=clock+1 WHERE id=1;
INSERT INTO sync_outbox(table_name,record_key,seq,payload) VALUES('study_sessions',json_array(OLD."id"),(SELECT clock FROM sync_context WHERE id=1),NULL) ON CONFLICT(table_name,record_key) DO UPDATE SET seq=excluded.seq,payload=excluded.payload;
END;
INSERT INTO sync_outbox SELECT 'study_sessions',json_array(r."id"),0,json_object('id',r."id",'course_id',r."course_id",'exam_id',r."exam_id",'lecture_id',r."lecture_id",'reading_id',r."reading_id",'task_id',r."task_id",'title',r."title",'started_at',r."started_at",'ended_at',r."ended_at",'duration_seconds',r."duration_seconds",'notes',r."notes",'created_at',r."created_at",'updated_at',r."updated_at") FROM "study_sessions" r;
CREATE TRIGGER sync_study_blocks_insert AFTER INSERT ON "study_blocks" WHEN (SELECT applying FROM sync_context WHERE id=1)=0 BEGIN
UPDATE sync_context SET clock=clock+1 WHERE id=1;
INSERT INTO sync_outbox(table_name,record_key,seq,payload) VALUES('study_blocks',json_array(NEW."id"),(SELECT clock FROM sync_context WHERE id=1),json_object('id',NEW."id",'course_id',NEW."course_id",'exam_id',NEW."exam_id",'lecture_id',NEW."lecture_id",'reading_id',NEW."reading_id",'task_id',NEW."task_id",'title',NEW."title",'scheduled_date',NEW."scheduled_date",'start_time',NEW."start_time",'estimated_minutes',NEW."estimated_minutes",'completed',NEW."completed",'completed_at',NEW."completed_at",'notes',NEW."notes",'created_at',NEW."created_at",'updated_at',NEW."updated_at")) ON CONFLICT(table_name,record_key) DO UPDATE SET seq=excluded.seq,payload=excluded.payload;
END;
CREATE TRIGGER sync_study_blocks_update AFTER UPDATE ON "study_blocks" WHEN (SELECT applying FROM sync_context WHERE id=1)=0 AND (OLD."id" IS NOT NEW."id" OR OLD."course_id" IS NOT NEW."course_id" OR OLD."exam_id" IS NOT NEW."exam_id" OR OLD."lecture_id" IS NOT NEW."lecture_id" OR OLD."reading_id" IS NOT NEW."reading_id" OR OLD."task_id" IS NOT NEW."task_id" OR OLD."title" IS NOT NEW."title" OR OLD."scheduled_date" IS NOT NEW."scheduled_date" OR OLD."start_time" IS NOT NEW."start_time" OR OLD."estimated_minutes" IS NOT NEW."estimated_minutes" OR OLD."completed" IS NOT NEW."completed" OR OLD."completed_at" IS NOT NEW."completed_at" OR OLD."notes" IS NOT NEW."notes" OR OLD."created_at" IS NOT NEW."created_at" OR OLD."updated_at" IS NOT NEW."updated_at") BEGIN
UPDATE sync_context SET clock=clock+1 WHERE id=1;
INSERT INTO sync_outbox(table_name,record_key,seq,payload) VALUES('study_blocks',json_array(NEW."id"),(SELECT clock FROM sync_context WHERE id=1),json_object('id',NEW."id",'course_id',NEW."course_id",'exam_id',NEW."exam_id",'lecture_id',NEW."lecture_id",'reading_id',NEW."reading_id",'task_id',NEW."task_id",'title',NEW."title",'scheduled_date',NEW."scheduled_date",'start_time',NEW."start_time",'estimated_minutes',NEW."estimated_minutes",'completed',NEW."completed",'completed_at',NEW."completed_at",'notes',NEW."notes",'created_at',NEW."created_at",'updated_at',NEW."updated_at")) ON CONFLICT(table_name,record_key) DO UPDATE SET seq=excluded.seq,payload=excluded.payload;
END;
CREATE TRIGGER sync_study_blocks_delete AFTER DELETE ON "study_blocks" WHEN (SELECT applying FROM sync_context WHERE id=1)=0 BEGIN
UPDATE sync_context SET clock=clock+1 WHERE id=1;
INSERT INTO sync_outbox(table_name,record_key,seq,payload) VALUES('study_blocks',json_array(OLD."id"),(SELECT clock FROM sync_context WHERE id=1),NULL) ON CONFLICT(table_name,record_key) DO UPDATE SET seq=excluded.seq,payload=excluded.payload;
END;
INSERT INTO sync_outbox SELECT 'study_blocks',json_array(r."id"),0,json_object('id',r."id",'course_id',r."course_id",'exam_id',r."exam_id",'lecture_id',r."lecture_id",'reading_id',r."reading_id",'task_id',r."task_id",'title',r."title",'scheduled_date',r."scheduled_date",'start_time',r."start_time",'estimated_minutes',r."estimated_minutes",'completed',r."completed",'completed_at',r."completed_at",'notes',r."notes",'created_at',r."created_at",'updated_at',r."updated_at") FROM "study_blocks" r;
CREATE TRIGGER sync_lecture_files_insert AFTER INSERT ON "lecture_files" WHEN (SELECT applying FROM sync_context WHERE id=1)=0 BEGIN
UPDATE sync_context SET clock=clock+1 WHERE id=1;
INSERT INTO sync_outbox(table_name,record_key,seq,payload) VALUES('lecture_files',json_array(NEW."lecture_id",NEW."file_id"),(SELECT clock FROM sync_context WHERE id=1),json_object('lecture_id',NEW."lecture_id",'file_id',NEW."file_id")) ON CONFLICT(table_name,record_key) DO UPDATE SET seq=excluded.seq,payload=excluded.payload;
END;
CREATE TRIGGER sync_lecture_files_update AFTER UPDATE ON "lecture_files" WHEN (SELECT applying FROM sync_context WHERE id=1)=0 AND (OLD."lecture_id" IS NOT NEW."lecture_id" OR OLD."file_id" IS NOT NEW."file_id") BEGIN
UPDATE sync_context SET clock=clock+1 WHERE id=1;
INSERT INTO sync_outbox(table_name,record_key,seq,payload) VALUES('lecture_files',json_array(NEW."lecture_id",NEW."file_id"),(SELECT clock FROM sync_context WHERE id=1),json_object('lecture_id',NEW."lecture_id",'file_id',NEW."file_id")) ON CONFLICT(table_name,record_key) DO UPDATE SET seq=excluded.seq,payload=excluded.payload;
END;
CREATE TRIGGER sync_lecture_files_delete AFTER DELETE ON "lecture_files" WHEN (SELECT applying FROM sync_context WHERE id=1)=0 BEGIN
UPDATE sync_context SET clock=clock+1 WHERE id=1;
INSERT INTO sync_outbox(table_name,record_key,seq,payload) VALUES('lecture_files',json_array(OLD."lecture_id",OLD."file_id"),(SELECT clock FROM sync_context WHERE id=1),NULL) ON CONFLICT(table_name,record_key) DO UPDATE SET seq=excluded.seq,payload=excluded.payload;
END;
INSERT INTO sync_outbox SELECT 'lecture_files',json_array(r."lecture_id",r."file_id"),0,json_object('lecture_id',r."lecture_id",'file_id',r."file_id") FROM "lecture_files" r;
CREATE TRIGGER sync_grading_scales_insert AFTER INSERT ON "grading_scales" WHEN (SELECT applying FROM sync_context WHERE id=1)=0 BEGIN
UPDATE sync_context SET clock=clock+1 WHERE id=1;
INSERT INTO sync_outbox(table_name,record_key,seq,payload) VALUES('grading_scales',json_array(NEW."id"),(SELECT clock FROM sync_context WHERE id=1),json_object('id',NEW."id",'course_id',NEW."course_id",'semester_id',NEW."semester_id",'name',NEW."name",'entries',NEW."entries")) ON CONFLICT(table_name,record_key) DO UPDATE SET seq=excluded.seq,payload=excluded.payload;
END;
CREATE TRIGGER sync_grading_scales_update AFTER UPDATE ON "grading_scales" WHEN (SELECT applying FROM sync_context WHERE id=1)=0 AND (OLD."id" IS NOT NEW."id" OR OLD."course_id" IS NOT NEW."course_id" OR OLD."semester_id" IS NOT NEW."semester_id" OR OLD."name" IS NOT NEW."name" OR OLD."entries" IS NOT NEW."entries") BEGIN
UPDATE sync_context SET clock=clock+1 WHERE id=1;
INSERT INTO sync_outbox(table_name,record_key,seq,payload) VALUES('grading_scales',json_array(NEW."id"),(SELECT clock FROM sync_context WHERE id=1),json_object('id',NEW."id",'course_id',NEW."course_id",'semester_id',NEW."semester_id",'name',NEW."name",'entries',NEW."entries")) ON CONFLICT(table_name,record_key) DO UPDATE SET seq=excluded.seq,payload=excluded.payload;
END;
CREATE TRIGGER sync_grading_scales_delete AFTER DELETE ON "grading_scales" WHEN (SELECT applying FROM sync_context WHERE id=1)=0 BEGIN
UPDATE sync_context SET clock=clock+1 WHERE id=1;
INSERT INTO sync_outbox(table_name,record_key,seq,payload) VALUES('grading_scales',json_array(OLD."id"),(SELECT clock FROM sync_context WHERE id=1),NULL) ON CONFLICT(table_name,record_key) DO UPDATE SET seq=excluded.seq,payload=excluded.payload;
END;
INSERT INTO sync_outbox SELECT 'grading_scales',json_array(r."id"),0,json_object('id',r."id",'course_id',r."course_id",'semester_id',r."semester_id",'name',r."name",'entries',r."entries") FROM "grading_scales" r;
CREATE TRIGGER sync_previous_exam_questions_insert AFTER INSERT ON "previous_exam_questions" WHEN (SELECT applying FROM sync_context WHERE id=1)=0 BEGIN
UPDATE sync_context SET clock=clock+1 WHERE id=1;
INSERT INTO sync_outbox(table_name,record_key,seq,payload) VALUES('previous_exam_questions',json_array(NEW."id"),(SELECT clock FROM sync_context WHERE id=1),json_object('id',NEW."id",'file_id',NEW."file_id",'course_id',NEW."course_id",'number',NEW."number",'text',NEW."text",'type',NEW."type",'year',NEW."year",'topics_json',NEW."topics_json",'citations_json',NEW."citations_json")) ON CONFLICT(table_name,record_key) DO UPDATE SET seq=excluded.seq,payload=excluded.payload;
END;
CREATE TRIGGER sync_previous_exam_questions_update AFTER UPDATE ON "previous_exam_questions" WHEN (SELECT applying FROM sync_context WHERE id=1)=0 AND (OLD."id" IS NOT NEW."id" OR OLD."file_id" IS NOT NEW."file_id" OR OLD."course_id" IS NOT NEW."course_id" OR OLD."number" IS NOT NEW."number" OR OLD."text" IS NOT NEW."text" OR OLD."type" IS NOT NEW."type" OR OLD."year" IS NOT NEW."year" OR OLD."topics_json" IS NOT NEW."topics_json" OR OLD."citations_json" IS NOT NEW."citations_json") BEGIN
UPDATE sync_context SET clock=clock+1 WHERE id=1;
INSERT INTO sync_outbox(table_name,record_key,seq,payload) VALUES('previous_exam_questions',json_array(NEW."id"),(SELECT clock FROM sync_context WHERE id=1),json_object('id',NEW."id",'file_id',NEW."file_id",'course_id',NEW."course_id",'number',NEW."number",'text',NEW."text",'type',NEW."type",'year',NEW."year",'topics_json',NEW."topics_json",'citations_json',NEW."citations_json")) ON CONFLICT(table_name,record_key) DO UPDATE SET seq=excluded.seq,payload=excluded.payload;
END;
CREATE TRIGGER sync_previous_exam_questions_delete AFTER DELETE ON "previous_exam_questions" WHEN (SELECT applying FROM sync_context WHERE id=1)=0 BEGIN
UPDATE sync_context SET clock=clock+1 WHERE id=1;
INSERT INTO sync_outbox(table_name,record_key,seq,payload) VALUES('previous_exam_questions',json_array(OLD."id"),(SELECT clock FROM sync_context WHERE id=1),NULL) ON CONFLICT(table_name,record_key) DO UPDATE SET seq=excluded.seq,payload=excluded.payload;
END;
INSERT INTO sync_outbox SELECT 'previous_exam_questions',json_array(r."id"),0,json_object('id',r."id",'file_id',r."file_id",'course_id',r."course_id",'number',r."number",'text',r."text",'type',r."type",'year',r."year",'topics_json',r."topics_json",'citations_json',r."citations_json") FROM "previous_exam_questions" r;
CREATE TRIGGER sync_professors_insert AFTER INSERT ON "professors" WHEN (SELECT applying FROM sync_context WHERE id=1)=0 BEGIN
UPDATE sync_context SET clock=clock+1 WHERE id=1;
INSERT INTO sync_outbox(table_name,record_key,seq,payload) VALUES('professors',json_array(NEW."id"),(SELECT clock FROM sync_context WHERE id=1),json_object('id',NEW."id",'name',NEW."name",'email',NEW."email",'office',NEW."office",'office_hours',NEW."office_hours",'department',NEW."department",'notes',NEW."notes")) ON CONFLICT(table_name,record_key) DO UPDATE SET seq=excluded.seq,payload=excluded.payload;
END;
CREATE TRIGGER sync_professors_update AFTER UPDATE ON "professors" WHEN (SELECT applying FROM sync_context WHERE id=1)=0 AND (OLD."id" IS NOT NEW."id" OR OLD."name" IS NOT NEW."name" OR OLD."email" IS NOT NEW."email" OR OLD."office" IS NOT NEW."office" OR OLD."office_hours" IS NOT NEW."office_hours" OR OLD."department" IS NOT NEW."department" OR OLD."notes" IS NOT NEW."notes") BEGIN
UPDATE sync_context SET clock=clock+1 WHERE id=1;
INSERT INTO sync_outbox(table_name,record_key,seq,payload) VALUES('professors',json_array(NEW."id"),(SELECT clock FROM sync_context WHERE id=1),json_object('id',NEW."id",'name',NEW."name",'email',NEW."email",'office',NEW."office",'office_hours',NEW."office_hours",'department',NEW."department",'notes',NEW."notes")) ON CONFLICT(table_name,record_key) DO UPDATE SET seq=excluded.seq,payload=excluded.payload;
END;
CREATE TRIGGER sync_professors_delete AFTER DELETE ON "professors" WHEN (SELECT applying FROM sync_context WHERE id=1)=0 BEGIN
UPDATE sync_context SET clock=clock+1 WHERE id=1;
INSERT INTO sync_outbox(table_name,record_key,seq,payload) VALUES('professors',json_array(OLD."id"),(SELECT clock FROM sync_context WHERE id=1),NULL) ON CONFLICT(table_name,record_key) DO UPDATE SET seq=excluded.seq,payload=excluded.payload;
END;
INSERT INTO sync_outbox SELECT 'professors',json_array(r."id"),0,json_object('id',r."id",'name',r."name",'email',r."email",'office',r."office",'office_hours',r."office_hours",'department',r."department",'notes',r."notes") FROM "professors" r;
CREATE TRIGGER sync_course_professors_insert AFTER INSERT ON "course_professors" WHEN (SELECT applying FROM sync_context WHERE id=1)=0 BEGIN
UPDATE sync_context SET clock=clock+1 WHERE id=1;
INSERT INTO sync_outbox(table_name,record_key,seq,payload) VALUES('course_professors',json_array(NEW."course_id",NEW."professor_id"),(SELECT clock FROM sync_context WHERE id=1),json_object('course_id',NEW."course_id",'professor_id',NEW."professor_id")) ON CONFLICT(table_name,record_key) DO UPDATE SET seq=excluded.seq,payload=excluded.payload;
END;
CREATE TRIGGER sync_course_professors_update AFTER UPDATE ON "course_professors" WHEN (SELECT applying FROM sync_context WHERE id=1)=0 AND (OLD."course_id" IS NOT NEW."course_id" OR OLD."professor_id" IS NOT NEW."professor_id") BEGIN
UPDATE sync_context SET clock=clock+1 WHERE id=1;
INSERT INTO sync_outbox(table_name,record_key,seq,payload) VALUES('course_professors',json_array(NEW."course_id",NEW."professor_id"),(SELECT clock FROM sync_context WHERE id=1),json_object('course_id',NEW."course_id",'professor_id',NEW."professor_id")) ON CONFLICT(table_name,record_key) DO UPDATE SET seq=excluded.seq,payload=excluded.payload;
END;
CREATE TRIGGER sync_course_professors_delete AFTER DELETE ON "course_professors" WHEN (SELECT applying FROM sync_context WHERE id=1)=0 BEGIN
UPDATE sync_context SET clock=clock+1 WHERE id=1;
INSERT INTO sync_outbox(table_name,record_key,seq,payload) VALUES('course_professors',json_array(OLD."course_id",OLD."professor_id"),(SELECT clock FROM sync_context WHERE id=1),NULL) ON CONFLICT(table_name,record_key) DO UPDATE SET seq=excluded.seq,payload=excluded.payload;
END;
INSERT INTO sync_outbox SELECT 'course_professors',json_array(r."course_id",r."professor_id"),0,json_object('course_id',r."course_id",'professor_id',r."professor_id") FROM "course_professors" r;
CREATE TRIGGER sync_degree_programs_insert AFTER INSERT ON "degree_programs" WHEN (SELECT applying FROM sync_context WHERE id=1)=0 BEGIN
UPDATE sync_context SET clock=clock+1 WHERE id=1;
INSERT INTO sync_outbox(table_name,record_key,seq,payload) VALUES('degree_programs',json_array(NEW."id"),(SELECT clock FROM sync_context WHERE id=1),json_object('id',NEW."id",'name',NEW."name",'degree_type',NEW."degree_type",'major',NEW."major",'minor',NEW."minor",'catalog_term',NEW."catalog_term",'evaluation_term',NEW."evaluation_term",'institution',NEW."institution",'total_credits',NEW."total_credits",'minimum_gpa',NEW."minimum_gpa",'reported_gpa',NEW."reported_gpa",'reported_used_credits',NEW."reported_used_credits",'revision',NEW."revision",'created_at',NEW."created_at",'updated_at',NEW."updated_at")) ON CONFLICT(table_name,record_key) DO UPDATE SET seq=excluded.seq,payload=excluded.payload;
END;
CREATE TRIGGER sync_degree_programs_update AFTER UPDATE ON "degree_programs" WHEN (SELECT applying FROM sync_context WHERE id=1)=0 AND (OLD."id" IS NOT NEW."id" OR OLD."name" IS NOT NEW."name" OR OLD."degree_type" IS NOT NEW."degree_type" OR OLD."major" IS NOT NEW."major" OR OLD."minor" IS NOT NEW."minor" OR OLD."catalog_term" IS NOT NEW."catalog_term" OR OLD."evaluation_term" IS NOT NEW."evaluation_term" OR OLD."institution" IS NOT NEW."institution" OR OLD."total_credits" IS NOT NEW."total_credits" OR OLD."minimum_gpa" IS NOT NEW."minimum_gpa" OR OLD."reported_gpa" IS NOT NEW."reported_gpa" OR OLD."reported_used_credits" IS NOT NEW."reported_used_credits" OR OLD."revision" IS NOT NEW."revision" OR OLD."created_at" IS NOT NEW."created_at" OR OLD."updated_at" IS NOT NEW."updated_at") BEGIN
UPDATE sync_context SET clock=clock+1 WHERE id=1;
INSERT INTO sync_outbox(table_name,record_key,seq,payload) VALUES('degree_programs',json_array(NEW."id"),(SELECT clock FROM sync_context WHERE id=1),json_object('id',NEW."id",'name',NEW."name",'degree_type',NEW."degree_type",'major',NEW."major",'minor',NEW."minor",'catalog_term',NEW."catalog_term",'evaluation_term',NEW."evaluation_term",'institution',NEW."institution",'total_credits',NEW."total_credits",'minimum_gpa',NEW."minimum_gpa",'reported_gpa',NEW."reported_gpa",'reported_used_credits',NEW."reported_used_credits",'revision',NEW."revision",'created_at',NEW."created_at",'updated_at',NEW."updated_at")) ON CONFLICT(table_name,record_key) DO UPDATE SET seq=excluded.seq,payload=excluded.payload;
END;
CREATE TRIGGER sync_degree_programs_delete AFTER DELETE ON "degree_programs" WHEN (SELECT applying FROM sync_context WHERE id=1)=0 BEGIN
UPDATE sync_context SET clock=clock+1 WHERE id=1;
INSERT INTO sync_outbox(table_name,record_key,seq,payload) VALUES('degree_programs',json_array(OLD."id"),(SELECT clock FROM sync_context WHERE id=1),NULL) ON CONFLICT(table_name,record_key) DO UPDATE SET seq=excluded.seq,payload=excluded.payload;
END;
INSERT INTO sync_outbox SELECT 'degree_programs',json_array(r."id"),0,json_object('id',r."id",'name',r."name",'degree_type',r."degree_type",'major',r."major",'minor',r."minor",'catalog_term',r."catalog_term",'evaluation_term',r."evaluation_term",'institution',r."institution",'total_credits',r."total_credits",'minimum_gpa',r."minimum_gpa",'reported_gpa',r."reported_gpa",'reported_used_credits',r."reported_used_credits",'revision',r."revision",'created_at',r."created_at",'updated_at',r."updated_at") FROM "degree_programs" r;
CREATE TRIGGER sync_degree_groups_insert AFTER INSERT ON "degree_groups" WHEN (SELECT applying FROM sync_context WHERE id=1)=0 BEGIN
UPDATE sync_context SET clock=clock+1 WHERE id=1;
INSERT INTO sync_outbox(table_name,record_key,seq,payload) VALUES('degree_groups',json_array(NEW."id"),(SELECT clock FROM sync_context WHERE id=1),json_object('id',NEW."id",'program_id',NEW."program_id",'parent_id',NEW."parent_id",'name',NEW."name",'description',NEW."description",'credits_required',NEW."credits_required",'minimum_gpa',NEW."minimum_gpa",'reported_used_credits',NEW."reported_used_credits",'reported_gpa',NEW."reported_gpa",'display_order',NEW."display_order",'completion_rule',NEW."completion_rule",'contributes_credits',NEW."contributes_credits")) ON CONFLICT(table_name,record_key) DO UPDATE SET seq=excluded.seq,payload=excluded.payload;
END;
CREATE TRIGGER sync_degree_groups_update AFTER UPDATE ON "degree_groups" WHEN (SELECT applying FROM sync_context WHERE id=1)=0 AND (OLD."id" IS NOT NEW."id" OR OLD."program_id" IS NOT NEW."program_id" OR OLD."parent_id" IS NOT NEW."parent_id" OR OLD."name" IS NOT NEW."name" OR OLD."description" IS NOT NEW."description" OR OLD."credits_required" IS NOT NEW."credits_required" OR OLD."minimum_gpa" IS NOT NEW."minimum_gpa" OR OLD."reported_used_credits" IS NOT NEW."reported_used_credits" OR OLD."reported_gpa" IS NOT NEW."reported_gpa" OR OLD."display_order" IS NOT NEW."display_order" OR OLD."completion_rule" IS NOT NEW."completion_rule" OR OLD."contributes_credits" IS NOT NEW."contributes_credits") BEGIN
UPDATE sync_context SET clock=clock+1 WHERE id=1;
INSERT INTO sync_outbox(table_name,record_key,seq,payload) VALUES('degree_groups',json_array(NEW."id"),(SELECT clock FROM sync_context WHERE id=1),json_object('id',NEW."id",'program_id',NEW."program_id",'parent_id',NEW."parent_id",'name',NEW."name",'description',NEW."description",'credits_required',NEW."credits_required",'minimum_gpa',NEW."minimum_gpa",'reported_used_credits',NEW."reported_used_credits",'reported_gpa',NEW."reported_gpa",'display_order',NEW."display_order",'completion_rule',NEW."completion_rule",'contributes_credits',NEW."contributes_credits")) ON CONFLICT(table_name,record_key) DO UPDATE SET seq=excluded.seq,payload=excluded.payload;
END;
CREATE TRIGGER sync_degree_groups_delete AFTER DELETE ON "degree_groups" WHEN (SELECT applying FROM sync_context WHERE id=1)=0 BEGIN
UPDATE sync_context SET clock=clock+1 WHERE id=1;
INSERT INTO sync_outbox(table_name,record_key,seq,payload) VALUES('degree_groups',json_array(OLD."id"),(SELECT clock FROM sync_context WHERE id=1),NULL) ON CONFLICT(table_name,record_key) DO UPDATE SET seq=excluded.seq,payload=excluded.payload;
END;
INSERT INTO sync_outbox SELECT 'degree_groups',json_array(r."id"),0,json_object('id',r."id",'program_id',r."program_id",'parent_id',r."parent_id",'name',r."name",'description',r."description",'credits_required',r."credits_required",'minimum_gpa',r."minimum_gpa",'reported_used_credits',r."reported_used_credits",'reported_gpa',r."reported_gpa",'display_order',r."display_order",'completion_rule',r."completion_rule",'contributes_credits',r."contributes_credits") FROM "degree_groups" r;
CREATE TRIGGER sync_degree_requirements_insert AFTER INSERT ON "degree_requirements" WHEN (SELECT applying FROM sync_context WHERE id=1)=0 BEGIN
UPDATE sync_context SET clock=clock+1 WHERE id=1;
INSERT INTO sync_outbox(table_name,record_key,seq,payload) VALUES('degree_requirements',json_array(NEW."id"),(SELECT clock FROM sync_context WHERE id=1),json_object('id',NEW."id",'group_id',NEW."group_id",'name',NEW."name",'kind',NEW."kind",'credits_required',NEW."credits_required",'count_required',NEW."count_required",'minimum_grade',NEW."minimum_grade",'minimum_level',NEW."minimum_level",'attribute',NEW."attribute",'manual_complete',NEW."manual_complete",'display_order',NEW."display_order")) ON CONFLICT(table_name,record_key) DO UPDATE SET seq=excluded.seq,payload=excluded.payload;
END;
CREATE TRIGGER sync_degree_requirements_update AFTER UPDATE ON "degree_requirements" WHEN (SELECT applying FROM sync_context WHERE id=1)=0 AND (OLD."id" IS NOT NEW."id" OR OLD."group_id" IS NOT NEW."group_id" OR OLD."name" IS NOT NEW."name" OR OLD."kind" IS NOT NEW."kind" OR OLD."credits_required" IS NOT NEW."credits_required" OR OLD."count_required" IS NOT NEW."count_required" OR OLD."minimum_grade" IS NOT NEW."minimum_grade" OR OLD."minimum_level" IS NOT NEW."minimum_level" OR OLD."attribute" IS NOT NEW."attribute" OR OLD."manual_complete" IS NOT NEW."manual_complete" OR OLD."display_order" IS NOT NEW."display_order") BEGIN
UPDATE sync_context SET clock=clock+1 WHERE id=1;
INSERT INTO sync_outbox(table_name,record_key,seq,payload) VALUES('degree_requirements',json_array(NEW."id"),(SELECT clock FROM sync_context WHERE id=1),json_object('id',NEW."id",'group_id',NEW."group_id",'name',NEW."name",'kind',NEW."kind",'credits_required',NEW."credits_required",'count_required',NEW."count_required",'minimum_grade',NEW."minimum_grade",'minimum_level',NEW."minimum_level",'attribute',NEW."attribute",'manual_complete',NEW."manual_complete",'display_order',NEW."display_order")) ON CONFLICT(table_name,record_key) DO UPDATE SET seq=excluded.seq,payload=excluded.payload;
END;
CREATE TRIGGER sync_degree_requirements_delete AFTER DELETE ON "degree_requirements" WHEN (SELECT applying FROM sync_context WHERE id=1)=0 BEGIN
UPDATE sync_context SET clock=clock+1 WHERE id=1;
INSERT INTO sync_outbox(table_name,record_key,seq,payload) VALUES('degree_requirements',json_array(OLD."id"),(SELECT clock FROM sync_context WHERE id=1),NULL) ON CONFLICT(table_name,record_key) DO UPDATE SET seq=excluded.seq,payload=excluded.payload;
END;
INSERT INTO sync_outbox SELECT 'degree_requirements',json_array(r."id"),0,json_object('id',r."id",'group_id',r."group_id",'name',r."name",'kind',r."kind",'credits_required',r."credits_required",'count_required',r."count_required",'minimum_grade',r."minimum_grade",'minimum_level',r."minimum_level",'attribute',r."attribute",'manual_complete',r."manual_complete",'display_order',r."display_order") FROM "degree_requirements" r;
CREATE TRIGGER sync_degree_options_insert AFTER INSERT ON "degree_options" WHEN (SELECT applying FROM sync_context WHERE id=1)=0 BEGIN
UPDATE sync_context SET clock=clock+1 WHERE id=1;
INSERT INTO sync_outbox(table_name,record_key,seq,payload) VALUES('degree_options',json_array(NEW."id"),(SELECT clock FROM sync_context WHERE id=1),json_object('id',NEW."id",'requirement_id',NEW."requirement_id",'subject',NEW."subject",'number',NEW."number")) ON CONFLICT(table_name,record_key) DO UPDATE SET seq=excluded.seq,payload=excluded.payload;
END;
CREATE TRIGGER sync_degree_options_update AFTER UPDATE ON "degree_options" WHEN (SELECT applying FROM sync_context WHERE id=1)=0 AND (OLD."id" IS NOT NEW."id" OR OLD."requirement_id" IS NOT NEW."requirement_id" OR OLD."subject" IS NOT NEW."subject" OR OLD."number" IS NOT NEW."number") BEGIN
UPDATE sync_context SET clock=clock+1 WHERE id=1;
INSERT INTO sync_outbox(table_name,record_key,seq,payload) VALUES('degree_options',json_array(NEW."id"),(SELECT clock FROM sync_context WHERE id=1),json_object('id',NEW."id",'requirement_id',NEW."requirement_id",'subject',NEW."subject",'number',NEW."number")) ON CONFLICT(table_name,record_key) DO UPDATE SET seq=excluded.seq,payload=excluded.payload;
END;
CREATE TRIGGER sync_degree_options_delete AFTER DELETE ON "degree_options" WHEN (SELECT applying FROM sync_context WHERE id=1)=0 BEGIN
UPDATE sync_context SET clock=clock+1 WHERE id=1;
INSERT INTO sync_outbox(table_name,record_key,seq,payload) VALUES('degree_options',json_array(OLD."id"),(SELECT clock FROM sync_context WHERE id=1),NULL) ON CONFLICT(table_name,record_key) DO UPDATE SET seq=excluded.seq,payload=excluded.payload;
END;
INSERT INTO sync_outbox SELECT 'degree_options',json_array(r."id"),0,json_object('id',r."id",'requirement_id',r."requirement_id",'subject',r."subject",'number',r."number") FROM "degree_options" r;
CREATE TRIGGER sync_degree_courses_insert AFTER INSERT ON "degree_courses" WHEN (SELECT applying FROM sync_context WHERE id=1)=0 BEGIN
UPDATE sync_context SET clock=clock+1 WHERE id=1;
INSERT INTO sync_outbox(table_name,record_key,seq,payload) VALUES('degree_courses',json_array(NEW."id"),(SELECT clock FROM sync_context WHERE id=1),json_object('id',NEW."id",'program_id',NEW."program_id",'subject',NEW."subject",'number',NEW."number",'title',NEW."title",'credits',NEW."credits",'grade',NEW."grade",'term',NEW."term",'original_term',NEW."original_term",'status',NEW."status",'source',NEW."source",'course_id',NEW."course_id",'excluded',NEW."excluded",'attributes',NEW."attributes")) ON CONFLICT(table_name,record_key) DO UPDATE SET seq=excluded.seq,payload=excluded.payload;
END;
CREATE TRIGGER sync_degree_courses_update AFTER UPDATE ON "degree_courses" WHEN (SELECT applying FROM sync_context WHERE id=1)=0 AND (OLD."id" IS NOT NEW."id" OR OLD."program_id" IS NOT NEW."program_id" OR OLD."subject" IS NOT NEW."subject" OR OLD."number" IS NOT NEW."number" OR OLD."title" IS NOT NEW."title" OR OLD."credits" IS NOT NEW."credits" OR OLD."grade" IS NOT NEW."grade" OR OLD."term" IS NOT NEW."term" OR OLD."original_term" IS NOT NEW."original_term" OR OLD."status" IS NOT NEW."status" OR OLD."source" IS NOT NEW."source" OR OLD."course_id" IS NOT NEW."course_id" OR OLD."excluded" IS NOT NEW."excluded" OR OLD."attributes" IS NOT NEW."attributes") BEGIN
UPDATE sync_context SET clock=clock+1 WHERE id=1;
INSERT INTO sync_outbox(table_name,record_key,seq,payload) VALUES('degree_courses',json_array(NEW."id"),(SELECT clock FROM sync_context WHERE id=1),json_object('id',NEW."id",'program_id',NEW."program_id",'subject',NEW."subject",'number',NEW."number",'title',NEW."title",'credits',NEW."credits",'grade',NEW."grade",'term',NEW."term",'original_term',NEW."original_term",'status',NEW."status",'source',NEW."source",'course_id',NEW."course_id",'excluded',NEW."excluded",'attributes',NEW."attributes")) ON CONFLICT(table_name,record_key) DO UPDATE SET seq=excluded.seq,payload=excluded.payload;
END;
CREATE TRIGGER sync_degree_courses_delete AFTER DELETE ON "degree_courses" WHEN (SELECT applying FROM sync_context WHERE id=1)=0 BEGIN
UPDATE sync_context SET clock=clock+1 WHERE id=1;
INSERT INTO sync_outbox(table_name,record_key,seq,payload) VALUES('degree_courses',json_array(OLD."id"),(SELECT clock FROM sync_context WHERE id=1),NULL) ON CONFLICT(table_name,record_key) DO UPDATE SET seq=excluded.seq,payload=excluded.payload;
END;
INSERT INTO sync_outbox SELECT 'degree_courses',json_array(r."id"),0,json_object('id',r."id",'program_id',r."program_id",'subject',r."subject",'number',r."number",'title',r."title",'credits',r."credits",'grade',r."grade",'term',r."term",'original_term',r."original_term",'status',r."status",'source',r."source",'course_id',r."course_id",'excluded',r."excluded",'attributes',r."attributes") FROM "degree_courses" r;
CREATE TRIGGER sync_degree_allocations_insert AFTER INSERT ON "degree_allocations" WHEN (SELECT applying FROM sync_context WHERE id=1)=0 BEGIN
UPDATE sync_context SET clock=clock+1 WHERE id=1;
INSERT INTO sync_outbox(table_name,record_key,seq,payload) VALUES('degree_allocations',json_array(NEW."requirement_id",NEW."course_id"),(SELECT clock FROM sync_context WHERE id=1),json_object('requirement_id',NEW."requirement_id",'course_id',NEW."course_id")) ON CONFLICT(table_name,record_key) DO UPDATE SET seq=excluded.seq,payload=excluded.payload;
END;
CREATE TRIGGER sync_degree_allocations_update AFTER UPDATE ON "degree_allocations" WHEN (SELECT applying FROM sync_context WHERE id=1)=0 AND (OLD."requirement_id" IS NOT NEW."requirement_id" OR OLD."course_id" IS NOT NEW."course_id") BEGIN
UPDATE sync_context SET clock=clock+1 WHERE id=1;
INSERT INTO sync_outbox(table_name,record_key,seq,payload) VALUES('degree_allocations',json_array(NEW."requirement_id",NEW."course_id"),(SELECT clock FROM sync_context WHERE id=1),json_object('requirement_id',NEW."requirement_id",'course_id',NEW."course_id")) ON CONFLICT(table_name,record_key) DO UPDATE SET seq=excluded.seq,payload=excluded.payload;
END;
CREATE TRIGGER sync_degree_allocations_delete AFTER DELETE ON "degree_allocations" WHEN (SELECT applying FROM sync_context WHERE id=1)=0 BEGIN
UPDATE sync_context SET clock=clock+1 WHERE id=1;
INSERT INTO sync_outbox(table_name,record_key,seq,payload) VALUES('degree_allocations',json_array(OLD."requirement_id",OLD."course_id"),(SELECT clock FROM sync_context WHERE id=1),NULL) ON CONFLICT(table_name,record_key) DO UPDATE SET seq=excluded.seq,payload=excluded.payload;
END;
INSERT INTO sync_outbox SELECT 'degree_allocations',json_array(r."requirement_id",r."course_id"),0,json_object('requirement_id',r."requirement_id",'course_id',r."course_id") FROM "degree_allocations" r;
CREATE TRIGGER sync_degree_plans_insert AFTER INSERT ON "degree_plans" WHEN (SELECT applying FROM sync_context WHERE id=1)=0 BEGIN
UPDATE sync_context SET clock=clock+1 WHERE id=1;
INSERT INTO sync_outbox(table_name,record_key,seq,payload) VALUES('degree_plans',json_array(NEW."id"),(SELECT clock FROM sync_context WHERE id=1),json_object('id',NEW."id",'requirement_id',NEW."requirement_id",'term',NEW."term",'semester_id',NEW."semester_id",'subject',NEW."subject",'number',NEW."number",'credits',NEW."credits",'override_prerequisites',NEW."override_prerequisites")) ON CONFLICT(table_name,record_key) DO UPDATE SET seq=excluded.seq,payload=excluded.payload;
END;
CREATE TRIGGER sync_degree_plans_update AFTER UPDATE ON "degree_plans" WHEN (SELECT applying FROM sync_context WHERE id=1)=0 AND (OLD."id" IS NOT NEW."id" OR OLD."requirement_id" IS NOT NEW."requirement_id" OR OLD."term" IS NOT NEW."term" OR OLD."semester_id" IS NOT NEW."semester_id" OR OLD."subject" IS NOT NEW."subject" OR OLD."number" IS NOT NEW."number" OR OLD."credits" IS NOT NEW."credits" OR OLD."override_prerequisites" IS NOT NEW."override_prerequisites") BEGIN
UPDATE sync_context SET clock=clock+1 WHERE id=1;
INSERT INTO sync_outbox(table_name,record_key,seq,payload) VALUES('degree_plans',json_array(NEW."id"),(SELECT clock FROM sync_context WHERE id=1),json_object('id',NEW."id",'requirement_id',NEW."requirement_id",'term',NEW."term",'semester_id',NEW."semester_id",'subject',NEW."subject",'number',NEW."number",'credits',NEW."credits",'override_prerequisites',NEW."override_prerequisites")) ON CONFLICT(table_name,record_key) DO UPDATE SET seq=excluded.seq,payload=excluded.payload;
END;
CREATE TRIGGER sync_degree_plans_delete AFTER DELETE ON "degree_plans" WHEN (SELECT applying FROM sync_context WHERE id=1)=0 BEGIN
UPDATE sync_context SET clock=clock+1 WHERE id=1;
INSERT INTO sync_outbox(table_name,record_key,seq,payload) VALUES('degree_plans',json_array(OLD."id"),(SELECT clock FROM sync_context WHERE id=1),NULL) ON CONFLICT(table_name,record_key) DO UPDATE SET seq=excluded.seq,payload=excluded.payload;
END;
INSERT INTO sync_outbox SELECT 'degree_plans',json_array(r."id"),0,json_object('id',r."id",'requirement_id',r."requirement_id",'term',r."term",'semester_id',r."semester_id",'subject',r."subject",'number',r."number",'credits',r."credits",'override_prerequisites',r."override_prerequisites") FROM "degree_plans" r;
CREATE TRIGGER sync_degree_prerequisites_insert AFTER INSERT ON "degree_prerequisites" WHEN (SELECT applying FROM sync_context WHERE id=1)=0 BEGIN
UPDATE sync_context SET clock=clock+1 WHERE id=1;
INSERT INTO sync_outbox(table_name,record_key,seq,payload) VALUES('degree_prerequisites',json_array(NEW."id"),(SELECT clock FROM sync_context WHERE id=1),json_object('id',NEW."id",'program_id',NEW."program_id",'subject',NEW."subject",'number',NEW."number",'prerequisite_subject',NEW."prerequisite_subject",'prerequisite_number',NEW."prerequisite_number",'relation',NEW."relation",'minimum_grade',NEW."minimum_grade")) ON CONFLICT(table_name,record_key) DO UPDATE SET seq=excluded.seq,payload=excluded.payload;
END;
CREATE TRIGGER sync_degree_prerequisites_update AFTER UPDATE ON "degree_prerequisites" WHEN (SELECT applying FROM sync_context WHERE id=1)=0 AND (OLD."id" IS NOT NEW."id" OR OLD."program_id" IS NOT NEW."program_id" OR OLD."subject" IS NOT NEW."subject" OR OLD."number" IS NOT NEW."number" OR OLD."prerequisite_subject" IS NOT NEW."prerequisite_subject" OR OLD."prerequisite_number" IS NOT NEW."prerequisite_number" OR OLD."relation" IS NOT NEW."relation" OR OLD."minimum_grade" IS NOT NEW."minimum_grade") BEGIN
UPDATE sync_context SET clock=clock+1 WHERE id=1;
INSERT INTO sync_outbox(table_name,record_key,seq,payload) VALUES('degree_prerequisites',json_array(NEW."id"),(SELECT clock FROM sync_context WHERE id=1),json_object('id',NEW."id",'program_id',NEW."program_id",'subject',NEW."subject",'number',NEW."number",'prerequisite_subject',NEW."prerequisite_subject",'prerequisite_number',NEW."prerequisite_number",'relation',NEW."relation",'minimum_grade',NEW."minimum_grade")) ON CONFLICT(table_name,record_key) DO UPDATE SET seq=excluded.seq,payload=excluded.payload;
END;
CREATE TRIGGER sync_degree_prerequisites_delete AFTER DELETE ON "degree_prerequisites" WHEN (SELECT applying FROM sync_context WHERE id=1)=0 BEGIN
UPDATE sync_context SET clock=clock+1 WHERE id=1;
INSERT INTO sync_outbox(table_name,record_key,seq,payload) VALUES('degree_prerequisites',json_array(OLD."id"),(SELECT clock FROM sync_context WHERE id=1),NULL) ON CONFLICT(table_name,record_key) DO UPDATE SET seq=excluded.seq,payload=excluded.payload;
END;
INSERT INTO sync_outbox SELECT 'degree_prerequisites',json_array(r."id"),0,json_object('id',r."id",'program_id',r."program_id",'subject',r."subject",'number',r."number",'prerequisite_subject',r."prerequisite_subject",'prerequisite_number',r."prerequisite_number",'relation',r."relation",'minimum_grade',r."minimum_grade") FROM "degree_prerequisites" r;
CREATE TRIGGER sync_local_import_sources_insert AFTER INSERT ON "local_import_sources" WHEN (SELECT applying FROM sync_context WHERE id=1)=0 BEGIN
UPDATE sync_context SET clock=clock+1 WHERE id=1;
INSERT INTO sync_outbox(table_name,record_key,seq,payload) VALUES('local_import_sources',json_array(NEW."id"),(SELECT clock FROM sync_context WHERE id=1),json_object('id',NEW."id",'kind',NEW."kind",'program_id',NEW."program_id",'course_id',NEW."course_id",'filename',NEW."filename",'parser_version',NEW."parser_version",'file_id',NEW."file_id",'content_hash',NEW."content_hash",'imported_at',NEW."imported_at",'changes_json',NEW."changes_json")) ON CONFLICT(table_name,record_key) DO UPDATE SET seq=excluded.seq,payload=excluded.payload;
END;
CREATE TRIGGER sync_local_import_sources_update AFTER UPDATE ON "local_import_sources" WHEN (SELECT applying FROM sync_context WHERE id=1)=0 AND (OLD."id" IS NOT NEW."id" OR OLD."kind" IS NOT NEW."kind" OR OLD."program_id" IS NOT NEW."program_id" OR OLD."course_id" IS NOT NEW."course_id" OR OLD."filename" IS NOT NEW."filename" OR OLD."parser_version" IS NOT NEW."parser_version" OR OLD."file_id" IS NOT NEW."file_id" OR OLD."content_hash" IS NOT NEW."content_hash" OR OLD."imported_at" IS NOT NEW."imported_at" OR OLD."changes_json" IS NOT NEW."changes_json") BEGIN
UPDATE sync_context SET clock=clock+1 WHERE id=1;
INSERT INTO sync_outbox(table_name,record_key,seq,payload) VALUES('local_import_sources',json_array(NEW."id"),(SELECT clock FROM sync_context WHERE id=1),json_object('id',NEW."id",'kind',NEW."kind",'program_id',NEW."program_id",'course_id',NEW."course_id",'filename',NEW."filename",'parser_version',NEW."parser_version",'file_id',NEW."file_id",'content_hash',NEW."content_hash",'imported_at',NEW."imported_at",'changes_json',NEW."changes_json")) ON CONFLICT(table_name,record_key) DO UPDATE SET seq=excluded.seq,payload=excluded.payload;
END;
CREATE TRIGGER sync_local_import_sources_delete AFTER DELETE ON "local_import_sources" WHEN (SELECT applying FROM sync_context WHERE id=1)=0 BEGIN
UPDATE sync_context SET clock=clock+1 WHERE id=1;
INSERT INTO sync_outbox(table_name,record_key,seq,payload) VALUES('local_import_sources',json_array(OLD."id"),(SELECT clock FROM sync_context WHERE id=1),NULL) ON CONFLICT(table_name,record_key) DO UPDATE SET seq=excluded.seq,payload=excluded.payload;
END;
INSERT INTO sync_outbox SELECT 'local_import_sources',json_array(r."id"),0,json_object('id',r."id",'kind',r."kind",'program_id',r."program_id",'course_id',r."course_id",'filename',r."filename",'parser_version',r."parser_version",'file_id',r."file_id",'content_hash',r."content_hash",'imported_at',r."imported_at",'changes_json',r."changes_json") FROM "local_import_sources" r;
CREATE TRIGGER sync_local_import_records_insert AFTER INSERT ON "local_import_records" WHEN (SELECT applying FROM sync_context WHERE id=1)=0 BEGIN
UPDATE sync_context SET clock=clock+1 WHERE id=1;
INSERT INTO sync_outbox(table_name,record_key,seq,payload) VALUES('local_import_records',json_array(NEW."source_id",NEW."entity_table",NEW."entity_id"),(SELECT clock FROM sync_context WHERE id=1),json_object('source_id',NEW."source_id",'entity_table',NEW."entity_table",'entity_id',NEW."entity_id",'before_json',NEW."before_json",'after_json',NEW."after_json")) ON CONFLICT(table_name,record_key) DO UPDATE SET seq=excluded.seq,payload=excluded.payload;
END;
CREATE TRIGGER sync_local_import_records_update AFTER UPDATE ON "local_import_records" WHEN (SELECT applying FROM sync_context WHERE id=1)=0 AND (OLD."source_id" IS NOT NEW."source_id" OR OLD."entity_table" IS NOT NEW."entity_table" OR OLD."entity_id" IS NOT NEW."entity_id" OR OLD."before_json" IS NOT NEW."before_json" OR OLD."after_json" IS NOT NEW."after_json") BEGIN
UPDATE sync_context SET clock=clock+1 WHERE id=1;
INSERT INTO sync_outbox(table_name,record_key,seq,payload) VALUES('local_import_records',json_array(NEW."source_id",NEW."entity_table",NEW."entity_id"),(SELECT clock FROM sync_context WHERE id=1),json_object('source_id',NEW."source_id",'entity_table',NEW."entity_table",'entity_id',NEW."entity_id",'before_json',NEW."before_json",'after_json',NEW."after_json")) ON CONFLICT(table_name,record_key) DO UPDATE SET seq=excluded.seq,payload=excluded.payload;
END;
CREATE TRIGGER sync_local_import_records_delete AFTER DELETE ON "local_import_records" WHEN (SELECT applying FROM sync_context WHERE id=1)=0 BEGIN
UPDATE sync_context SET clock=clock+1 WHERE id=1;
INSERT INTO sync_outbox(table_name,record_key,seq,payload) VALUES('local_import_records',json_array(OLD."source_id",OLD."entity_table",OLD."entity_id"),(SELECT clock FROM sync_context WHERE id=1),NULL) ON CONFLICT(table_name,record_key) DO UPDATE SET seq=excluded.seq,payload=excluded.payload;
END;
INSERT INTO sync_outbox SELECT 'local_import_records',json_array(r."source_id",r."entity_table",r."entity_id"),0,json_object('source_id',r."source_id",'entity_table',r."entity_table",'entity_id',r."entity_id",'before_json',r."before_json",'after_json',r."after_json") FROM "local_import_records" r;
CREATE TRIGGER sync_degree_import_snapshots_insert AFTER INSERT ON "degree_import_snapshots" WHEN (SELECT applying FROM sync_context WHERE id=1)=0 BEGIN
UPDATE sync_context SET clock=clock+1 WHERE id=1;
INSERT INTO sync_outbox(table_name,record_key,seq,payload) VALUES('degree_import_snapshots',json_array(NEW."source_id"),(SELECT clock FROM sync_context WHERE id=1),json_object('source_id',NEW."source_id",'before_json',NEW."before_json",'after_json',NEW."after_json")) ON CONFLICT(table_name,record_key) DO UPDATE SET seq=excluded.seq,payload=excluded.payload;
END;
CREATE TRIGGER sync_degree_import_snapshots_update AFTER UPDATE ON "degree_import_snapshots" WHEN (SELECT applying FROM sync_context WHERE id=1)=0 AND (OLD."source_id" IS NOT NEW."source_id" OR OLD."before_json" IS NOT NEW."before_json" OR OLD."after_json" IS NOT NEW."after_json") BEGIN
UPDATE sync_context SET clock=clock+1 WHERE id=1;
INSERT INTO sync_outbox(table_name,record_key,seq,payload) VALUES('degree_import_snapshots',json_array(NEW."source_id"),(SELECT clock FROM sync_context WHERE id=1),json_object('source_id',NEW."source_id",'before_json',NEW."before_json",'after_json',NEW."after_json")) ON CONFLICT(table_name,record_key) DO UPDATE SET seq=excluded.seq,payload=excluded.payload;
END;
CREATE TRIGGER sync_degree_import_snapshots_delete AFTER DELETE ON "degree_import_snapshots" WHEN (SELECT applying FROM sync_context WHERE id=1)=0 BEGIN
UPDATE sync_context SET clock=clock+1 WHERE id=1;
INSERT INTO sync_outbox(table_name,record_key,seq,payload) VALUES('degree_import_snapshots',json_array(OLD."source_id"),(SELECT clock FROM sync_context WHERE id=1),NULL) ON CONFLICT(table_name,record_key) DO UPDATE SET seq=excluded.seq,payload=excluded.payload;
END;
INSERT INTO sync_outbox SELECT 'degree_import_snapshots',json_array(r."source_id"),0,json_object('source_id',r."source_id",'before_json',r."before_json",'after_json',r."after_json") FROM "degree_import_snapshots" r;
CREATE TRIGGER sync_course_syllabus_insert AFTER INSERT ON "course_syllabus" WHEN (SELECT applying FROM sync_context WHERE id=1)=0 BEGIN
UPDATE sync_context SET clock=clock+1 WHERE id=1;
INSERT INTO sync_outbox(table_name,record_key,seq,payload) VALUES('course_syllabus',json_array(NEW."id"),(SELECT clock FROM sync_context WHERE id=1),json_object('id',NEW."id",'course_id',NEW."course_id",'description',NEW."description",'objectives',NEW."objectives",'source_file_id',NEW."source_file_id",'source_filename',NEW."source_filename",'parser_version',NEW."parser_version",'imported_at',NEW."imported_at",'updated_at',NEW."updated_at")) ON CONFLICT(table_name,record_key) DO UPDATE SET seq=excluded.seq,payload=excluded.payload;
END;
CREATE TRIGGER sync_course_syllabus_update AFTER UPDATE ON "course_syllabus" WHEN (SELECT applying FROM sync_context WHERE id=1)=0 AND (OLD."id" IS NOT NEW."id" OR OLD."course_id" IS NOT NEW."course_id" OR OLD."description" IS NOT NEW."description" OR OLD."objectives" IS NOT NEW."objectives" OR OLD."source_file_id" IS NOT NEW."source_file_id" OR OLD."source_filename" IS NOT NEW."source_filename" OR OLD."parser_version" IS NOT NEW."parser_version" OR OLD."imported_at" IS NOT NEW."imported_at" OR OLD."updated_at" IS NOT NEW."updated_at") BEGIN
UPDATE sync_context SET clock=clock+1 WHERE id=1;
INSERT INTO sync_outbox(table_name,record_key,seq,payload) VALUES('course_syllabus',json_array(NEW."id"),(SELECT clock FROM sync_context WHERE id=1),json_object('id',NEW."id",'course_id',NEW."course_id",'description',NEW."description",'objectives',NEW."objectives",'source_file_id',NEW."source_file_id",'source_filename',NEW."source_filename",'parser_version',NEW."parser_version",'imported_at',NEW."imported_at",'updated_at',NEW."updated_at")) ON CONFLICT(table_name,record_key) DO UPDATE SET seq=excluded.seq,payload=excluded.payload;
END;
CREATE TRIGGER sync_course_syllabus_delete AFTER DELETE ON "course_syllabus" WHEN (SELECT applying FROM sync_context WHERE id=1)=0 BEGIN
UPDATE sync_context SET clock=clock+1 WHERE id=1;
INSERT INTO sync_outbox(table_name,record_key,seq,payload) VALUES('course_syllabus',json_array(OLD."id"),(SELECT clock FROM sync_context WHERE id=1),NULL) ON CONFLICT(table_name,record_key) DO UPDATE SET seq=excluded.seq,payload=excluded.payload;
END;
INSERT INTO sync_outbox SELECT 'course_syllabus',json_array(r."id"),0,json_object('id',r."id",'course_id',r."course_id",'description',r."description",'objectives',r."objectives",'source_file_id',r."source_file_id",'source_filename',r."source_filename",'parser_version',r."parser_version",'imported_at',r."imported_at",'updated_at',r."updated_at") FROM "course_syllabus" r;
CREATE TRIGGER sync_course_syllabus_policy_insert AFTER INSERT ON "course_syllabus_policy" WHEN (SELECT applying FROM sync_context WHERE id=1)=0 BEGIN
UPDATE sync_context SET clock=clock+1 WHERE id=1;
INSERT INTO sync_outbox(table_name,record_key,seq,payload) VALUES('course_syllabus_policy',json_array(NEW."id"),(SELECT clock FROM sync_context WHERE id=1),json_object('id',NEW."id",'course_id',NEW."course_id",'category',NEW."category",'text',NEW."text",'sort_order',NEW."sort_order")) ON CONFLICT(table_name,record_key) DO UPDATE SET seq=excluded.seq,payload=excluded.payload;
END;
CREATE TRIGGER sync_course_syllabus_policy_update AFTER UPDATE ON "course_syllabus_policy" WHEN (SELECT applying FROM sync_context WHERE id=1)=0 AND (OLD."id" IS NOT NEW."id" OR OLD."course_id" IS NOT NEW."course_id" OR OLD."category" IS NOT NEW."category" OR OLD."text" IS NOT NEW."text" OR OLD."sort_order" IS NOT NEW."sort_order") BEGIN
UPDATE sync_context SET clock=clock+1 WHERE id=1;
INSERT INTO sync_outbox(table_name,record_key,seq,payload) VALUES('course_syllabus_policy',json_array(NEW."id"),(SELECT clock FROM sync_context WHERE id=1),json_object('id',NEW."id",'course_id',NEW."course_id",'category',NEW."category",'text',NEW."text",'sort_order',NEW."sort_order")) ON CONFLICT(table_name,record_key) DO UPDATE SET seq=excluded.seq,payload=excluded.payload;
END;
CREATE TRIGGER sync_course_syllabus_policy_delete AFTER DELETE ON "course_syllabus_policy" WHEN (SELECT applying FROM sync_context WHERE id=1)=0 BEGIN
UPDATE sync_context SET clock=clock+1 WHERE id=1;
INSERT INTO sync_outbox(table_name,record_key,seq,payload) VALUES('course_syllabus_policy',json_array(OLD."id"),(SELECT clock FROM sync_context WHERE id=1),NULL) ON CONFLICT(table_name,record_key) DO UPDATE SET seq=excluded.seq,payload=excluded.payload;
END;
INSERT INTO sync_outbox SELECT 'course_syllabus_policy',json_array(r."id"),0,json_object('id',r."id",'course_id',r."course_id",'category',r."category",'text',r."text",'sort_order',r."sort_order") FROM "course_syllabus_policy" r;
CREATE TRIGGER sync_course_syllabus_material_insert AFTER INSERT ON "course_syllabus_material" WHEN (SELECT applying FROM sync_context WHERE id=1)=0 BEGIN
UPDATE sync_context SET clock=clock+1 WHERE id=1;
INSERT INTO sync_outbox(table_name,record_key,seq,payload) VALUES('course_syllabus_material',json_array(NEW."id"),(SELECT clock FROM sync_context WHERE id=1),json_object('id',NEW."id",'course_id',NEW."course_id",'kind',NEW."kind",'title',NEW."title",'detail',NEW."detail",'sort_order',NEW."sort_order")) ON CONFLICT(table_name,record_key) DO UPDATE SET seq=excluded.seq,payload=excluded.payload;
END;
CREATE TRIGGER sync_course_syllabus_material_update AFTER UPDATE ON "course_syllabus_material" WHEN (SELECT applying FROM sync_context WHERE id=1)=0 AND (OLD."id" IS NOT NEW."id" OR OLD."course_id" IS NOT NEW."course_id" OR OLD."kind" IS NOT NEW."kind" OR OLD."title" IS NOT NEW."title" OR OLD."detail" IS NOT NEW."detail" OR OLD."sort_order" IS NOT NEW."sort_order") BEGIN
UPDATE sync_context SET clock=clock+1 WHERE id=1;
INSERT INTO sync_outbox(table_name,record_key,seq,payload) VALUES('course_syllabus_material',json_array(NEW."id"),(SELECT clock FROM sync_context WHERE id=1),json_object('id',NEW."id",'course_id',NEW."course_id",'kind',NEW."kind",'title',NEW."title",'detail',NEW."detail",'sort_order',NEW."sort_order")) ON CONFLICT(table_name,record_key) DO UPDATE SET seq=excluded.seq,payload=excluded.payload;
END;
CREATE TRIGGER sync_course_syllabus_material_delete AFTER DELETE ON "course_syllabus_material" WHEN (SELECT applying FROM sync_context WHERE id=1)=0 BEGIN
UPDATE sync_context SET clock=clock+1 WHERE id=1;
INSERT INTO sync_outbox(table_name,record_key,seq,payload) VALUES('course_syllabus_material',json_array(OLD."id"),(SELECT clock FROM sync_context WHERE id=1),NULL) ON CONFLICT(table_name,record_key) DO UPDATE SET seq=excluded.seq,payload=excluded.payload;
END;
INSERT INTO sync_outbox SELECT 'course_syllabus_material',json_array(r."id"),0,json_object('id',r."id",'course_id',r."course_id",'kind',r."kind",'title',r."title",'detail',r."detail",'sort_order',r."sort_order") FROM "course_syllabus_material" r;
CREATE TRIGGER sync_course_syllabus_office_hours_insert AFTER INSERT ON "course_syllabus_office_hours" WHEN (SELECT applying FROM sync_context WHERE id=1)=0 BEGIN
UPDATE sync_context SET clock=clock+1 WHERE id=1;
INSERT INTO sync_outbox(table_name,record_key,seq,payload) VALUES('course_syllabus_office_hours',json_array(NEW."id"),(SELECT clock FROM sync_context WHERE id=1),json_object('id',NEW."id",'course_id',NEW."course_id",'day_of_week',NEW."day_of_week",'start_time',NEW."start_time",'end_time',NEW."end_time",'location',NEW."location",'note',NEW."note")) ON CONFLICT(table_name,record_key) DO UPDATE SET seq=excluded.seq,payload=excluded.payload;
END;
CREATE TRIGGER sync_course_syllabus_office_hours_update AFTER UPDATE ON "course_syllabus_office_hours" WHEN (SELECT applying FROM sync_context WHERE id=1)=0 AND (OLD."id" IS NOT NEW."id" OR OLD."course_id" IS NOT NEW."course_id" OR OLD."day_of_week" IS NOT NEW."day_of_week" OR OLD."start_time" IS NOT NEW."start_time" OR OLD."end_time" IS NOT NEW."end_time" OR OLD."location" IS NOT NEW."location" OR OLD."note" IS NOT NEW."note") BEGIN
UPDATE sync_context SET clock=clock+1 WHERE id=1;
INSERT INTO sync_outbox(table_name,record_key,seq,payload) VALUES('course_syllabus_office_hours',json_array(NEW."id"),(SELECT clock FROM sync_context WHERE id=1),json_object('id',NEW."id",'course_id',NEW."course_id",'day_of_week',NEW."day_of_week",'start_time',NEW."start_time",'end_time',NEW."end_time",'location',NEW."location",'note',NEW."note")) ON CONFLICT(table_name,record_key) DO UPDATE SET seq=excluded.seq,payload=excluded.payload;
END;
CREATE TRIGGER sync_course_syllabus_office_hours_delete AFTER DELETE ON "course_syllabus_office_hours" WHEN (SELECT applying FROM sync_context WHERE id=1)=0 BEGIN
UPDATE sync_context SET clock=clock+1 WHERE id=1;
INSERT INTO sync_outbox(table_name,record_key,seq,payload) VALUES('course_syllabus_office_hours',json_array(OLD."id"),(SELECT clock FROM sync_context WHERE id=1),NULL) ON CONFLICT(table_name,record_key) DO UPDATE SET seq=excluded.seq,payload=excluded.payload;
END;
INSERT INTO sync_outbox SELECT 'course_syllabus_office_hours',json_array(r."id"),0,json_object('id',r."id",'course_id',r."course_id",'day_of_week',r."day_of_week",'start_time',r."start_time",'end_time',r."end_time",'location',r."location",'note',r."note") FROM "course_syllabus_office_hours" r;
